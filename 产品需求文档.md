# 端云协同训练推理平台 — 产品需求文档（PRD）

> 文档版本：v1.0 | 日期：2026-05-11 | 状态：初稿

---

## 1. 文档概述

### 1.1 目的

本文档定义"端云协同训练推理平台"的产品需求，作为产品设计与技术开发的基础输入。平台旨在为边缘 AI 场景提供从数据采集、模型训练、算法容器化到设备端部署与推理的**端到端、全生命周期管理能力**，对标业界 Edge Impulse、华为 ModelArts + MindX Edge、百度 EasyDL + BIE 等方案，同时避免厂商硬件锁定。

### 1.2 读者

- 产品经理、UI/UX 设计师
- 后端/前端/嵌入式开发工程师
- AI 算法工程师
- 测试工程师
- 项目管理与决策层

### 1.3 术语定义

| 术语 | 说明 |
| :--- | :--- |
| 云平台 | 面向用户的 Web/API 层，管理产品、项目、数据集、模型等业务实体 |
| 训练管理平台 | 后台服务层，编排训练任务、管理算法容器、与设备端交互 |
| 算法容器 | 封装训练/推理算法的 Docker 容器，作为可复用、可调度的计算单元 |
| 设备端 | 边缘/端侧硬件（MCU/MPU/NPU/边缘盒子），运行推理引擎并上报数据 |
| 端云协同 | 云侧与设备端通过安全通道协作完成训练数据回流、模型下发、联合推理等 |

---

## 2. 产品愿景与目标

### 2.1 愿景

构建一个**开放、可私有化部署**的端云协同 AI 平台，让企业与开发者能够：
- 在云侧用低代码/可视化方式完成数据管理与模型训练；
- 将训练好的模型自动容器化或编译为端侧可执行格式；
- 一键或策略驱动地将模型安全下发到异构边缘设备；
- 在设备端高效推理并实现数据回流与模型持续迭代。

### 2.2 核心目标

| # | 目标 | 量化指标（MVP） |
| :--- | :--- | :--- |
| G1 | 降低边缘 AI 开发门槛 | 非 AI 背景工程师可在 4h 内完成从数据上传到模型部署全流程 |
| G2 | 支持异构硬件 | MVP 阶段支持 ≥3 类硬件（如 NVIDIA Jetson、RK3588、STM32） |
| G3 | 训练-部署全自动化 | 从训练完成到设备端模型更新 ≤ 15 分钟（含 OTA） |
| G4 | 可私有化部署 | 支持纯内网环境运行，无外部依赖 |
| G5 | 数据安全与合规 | 端侧原始数据不强制上云，支持边缘预处理与脱敏 |

### 2.3 非目标（MVP 阶段不做）

- 联邦学习（需后续版本规划）
- 自研推理芯片/硬件
- SaaS 公有云商业化运营（初期面向私有化交付）
- 视频流实时转码与存储（由第三方系统完成）

---

## 3. 用户与角色

| 角色 | 描述 | 核心诉求 |
| :--- | :--- | :--- |
| **平台管理员** | 平台运维者 | 系统部署、资源配额、用户权限、集群管理 |
| **项目管理者** | 业务线负责人 | 创建产品/项目、分配成员、查看全局看板 |
| **算法工程师** | 模型开发者 | 上传/标注数据、选择/定制算法、训练调参、评估模型 |
| **应用开发者** | 设备端集成者 | 获取模型包/SDK、集成到设备、调试推理效果 |
| **设备运维** | 现场部署人员 | 设备注册、模型更新、状态监控、日志排查 |

---

## 4. 系统四层架构概述

```
┌─────────────────────────────────────────────────┐
│               云平台（Cloud Platform）             │
│  产品管理 │ 项目管理 │ 数据管理 │ 模型管理 │ 用户/权限  │
└────────────────────────┬────────────────────────┘
                         │ RESTful API / gRPC
┌────────────────────────▼────────────────────────┐
│         训练管理平台（Training Management）        │
│  训练编排 │ 算法仓库 │ 容器调度 │ 模型转换 │ 设备通信   │
└────────────────────────┬────────────────────────┘
                         │ Docker / K8s
┌────────────────────────▼────────────────────────┐
│            算法容器（Algorithm Containers）         │
│  训练容器 │ 评估容器 │ 转换/量化容器 │ 推理服务容器   │
└────────────────────────┬────────────────────────┘
                         │ MQTT / gRPC / OTA
┌────────────────────────▼────────────────────────┐
│              设备端（Edge Devices）                │
│  Agent │ 推理引擎 │ 数据采集 │ 本地存储 │ 安全模块    │
└─────────────────────────────────────────────────┘
```

---

## 5. 功能需求

### 5.1 云平台

#### 5.1.1 产品与项目管理

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| CP-001 | 产品创建与管理 | P0 | 支持创建"产品"作为顶层业务单元，关联多个项目 |
| CP-002 | 项目 CRUD | P0 | 每个项目包含数据集、模型、设备组、部署策略 |
| CP-003 | 团队成员管理 | P0 | 基于 RBAC 的成员邀请、角色分配 |
| CP-004 | 操作审计日志 | P1 | 记录所有关键操作（训练/部署/删除）含操作人、时间、对象 |
| CP-005 | 全局看板 | P1 | 项目进度、模型数量、设备在线率、训练任务统计 |

#### 5.1.2 数据管理

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| CD-001 | 数据集创建 | P0 | 支持图像、音频、传感器时序、文本等多类型数据集 |
| CD-002 | 数据上传 | P0 | Web 拖拽上传、API 批量上传、设备端自动回流 |
| CD-003 | 数据标注 | P0 | 内置标注工具（矩形框/多边形/关键点/分类标签/音频片段） |
| CD-004 | 标注任务管理 | P1 | 多人协作标注、进度追踪、质量抽检 |
| CD-005 | 数据集版本管理 | P1 | 类似 Git 的版本控制，支持 diff 与回滚 |
| CD-006 | 自动标注 / 预标注 | P2 | 利用已有模型对新数据进行预标注，人工审核修正 |
| CD-007 | 数据增强 | P1 | 内置常见增强策略（翻转/旋转/裁剪/加噪/混音等） |
| CD-008 | 数据统计与可视化 | P1 | 类别分布、样本数量、质量评分可视化 |

#### 5.1.3 模型管理

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| CM-001 | 模型仓库 | P0 | 存储训练产出模型，含版本、指标、格式、关联数据集 |
| CM-002 | 模型版本管理 | P0 | 每次训练产出新版本，支持对比、回滚、标记（staging/production） |
| CM-003 | 模型评估报告 | P0 | 展示精度、召回率、F1、混淆矩阵、推理时延 |
| CM-004 | 模型格式转换 | P0 | 支持 ONNX/TFLite/TensorRT/OpenVINO/NCNN 等格式导出 |
| CM-005 | 模型审批流 | P1 | 模型上线需经审批，支持多级审批策略 |
| CM-006 | 预训练模型库 | P1 | 内置常见基础模型（MobileNet/YOLO/ResNet/EfficientNet 等） |

#### 5.1.4 部署管理

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| CDY-001 | 部署策略定义 | P0 | 按设备组 / 标签 / 灰度百分比定义模型下发策略 |
| CDY-002 | 一键部署 | P0 | 选择模型版本 + 目标设备组 → 触发 OTA 下发 |
| CDY-003 | 灰度发布 | P1 | 按百分比逐步扩大部署范围，支持自动回滚 |
| CDY-004 | 部署状态追踪 | P0 | 每个设备的部署进度、成功/失败/超时状态 |
| CDY-005 | 回滚 | P0 | 一键回滚到上一个 Production 版本 |

---

### 5.2 训练管理平台

#### 5.2.1 训练任务编排

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| TM-001 | 训练任务创建 | P0 | 选择数据集 + 算法容器 + 超参数 → 创建训练任务 |
| TM-002 | 训练任务队列 | P0 | 支持 FIFO / 优先级队列，GPU/CPU 资源排队调度 |
| TM-003 | 分布式训练 | P2 | 支持多卡/多节点数据并行训练 |
| TM-004 | 超参数搜索 | P1 | 网格搜索 / 随机搜索 / Bayesian 优化 |
| TM-005 | 训练进度实时展示 | P0 | Loss/Acc 曲线实时推送，支持 TensorBoard 集成 |
| TM-006 | 训练日志 | P0 | 实时日志流 + 持久化存储，支持搜索 |
| TM-007 | 训练任务生命周期 | P0 | 创建 → 排队 → 运行 → 成功/失败/取消/超时 |
| TM-008 | 自动停止策略 | P1 | 早停（Early Stopping）、最大训练时长、资源超限自动终止 |

#### 5.2.2 算法仓库管理

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| AR-001 | 算法模板注册 | P0 | 将算法容器镜像注册到平台，含元数据（输入/输出/超参定义） |
| AR-002 | 算法版本管理 | P0 | 每个算法支持多版本，类似 Docker tag |
| AR-003 | 内置算法库 | P1 | 平台预置常见算法（图像分类/目标检测/异常检测/语音识别） |
| AR-004 | 自定义算法接入 | P0 | 按规范打包的自定义 Docker 镜像可注册为新算法 |
| AR-005 | 算法参数 Schema | P0 | 每个算法声明超参数 JSON Schema，云平台据此生成配置表单 |

#### 5.2.3 模型转换与优化

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| MC-001 | 格式转换 Pipeline | P0 | PyTorch/TF/Keras → ONNX → 目标格式（TFLite/TensorRT/OpenVINO/NCNN） |
| MC-002 | 量化 | P0 | 支持 INT8 后训练量化（PTQ）与量化感知训练（QAT） |
| MC-003 | 剪枝 | P1 | 结构化剪枝 + 微调 |
| MC-004 | 知识蒸馏 | P2 | 大模型 → 小模型蒸馏 |
| MC-005 | 目标设备性能评估 | P0 | 转换后在仿真或真机上获取 FPS/延迟/内存占用 |

#### 5.2.4 设备通信管理

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| DC-001 | 设备注册与认证 | P0 | 设备通过证书/Token 注册到平台，支持批量导入 |
| DC-002 | 设备分组与标签 | P0 | 按项目/地域/型号分组，打标签用于部署策略 |
| DC-003 | 设备状态监控 | P0 | 在线/离线/异常，CPU/内存/温度/推理 FPS |
| DC-004 | 模型 OTA 下发 | P0 | 增量/全量模型包下发，断点续传，校验 |
| DC-005 | 数据回流通道 | P1 | 设备端采集的数据/推理结果按策略上传到云端 |
| DC-006 | 远程调试 | P2 | SSH 隧道 / 远程终端 / 日志拉取 |
| DC-007 | 指令下发 | P0 | 云端向设备发送控制指令（重启推理/切换模型/采集数据） |

---

### 5.3 算法容器

#### 5.3.1 容器规范

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| AC-001 | 标准接口定义 | P0 | 定义统一的输入/输出目录、环境变量、回调接口 |
| AC-002 | 训练容器 | P0 | 读取数据目录 + 超参 → 输出模型文件 + 训练指标 |
| AC-003 | 评估容器 | P0 | 读取模型 + 测试集 → 输出评估报告（JSON） |
| AC-004 | 转换容器 | P0 | 读取源模型 → 输出目标格式模型 + 性能报告 |
| AC-005 | 推理服务容器 | P1 | 提供 gRPC/HTTP 推理 API，用于云侧在线推理或联合推理 |
| AC-006 | GPU/NPU 支持 | P0 | 容器可挂载 GPU（NVIDIA）或 NPU 驱动 |
| AC-007 | 资源限制 | P0 | 支持设置 CPU/Memory/GPU 配额 |

#### 5.3.2 容器生命周期

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| ACL-001 | 镜像构建 | P1 | 平台内置 CI 能力或对接外部 CI/CD 构建镜像 |
| ACL-002 | 镜像仓库 | P0 | 内置 Harbor/Registry 存储算法镜像 |
| ACL-003 | 容器健康检查 | P0 | 定义 liveness/readiness 探针 |
| ACL-004 | 日志采集 | P0 | stdout/stderr 自动采集到日志系统 |
| ACL-005 | 容器自动清理 | P1 | 任务完成/失败后自动回收容器资源 |

---

### 5.4 设备端

#### 5.4.1 设备 Agent

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| DA-001 | 轻量 Agent | P0 | C/C++/Rust 实现，资源占用 < 50MB RAM（MPU）/ < 256KB（MCU） |
| DA-002 | 安全通信 | P0 | TLS 1.3 + 双向证书认证，支持 MQTT/gRPC |
| DA-003 | 模型接收与校验 | P0 | 接收 OTA 模型包、完整性校验（SHA256）、原子切换 |
| DA-004 | 推理引擎管理 | P0 | 加载/卸载模型、切换模型版本、热更新（不中断推理） |
| DA-005 | 数据采集上报 | P1 | 按策略（定时/触发/采样率）采集推理输入/输出/异常样本 |
| DA-006 | 本地推理 | P0 | 集成 ONNX Runtime / TFLite / NCNN / TensorRT 等推理后端 |
| DA-007 | 离线运行 | P0 | 网络断开时正常推理，恢复后同步状态与数据 |
| DA-008 | 自我监控 | P1 | 心跳上报、资源使用（CPU/GPU/内存/温度）上报 |
| DA-009 | 固件/Agent OTA | P1 | Agent 自身的版本更新机制 |

#### 5.4.2 推理能力

| ID | 需求 | 优先级 | 描述 |
| :--- | :--- | :--- | :--- |
| DI-001 | 多推理后端 | P0 | 支持 ONNX Runtime、TFLite、TensorRT、NCNN、OpenVINO |
| DI-002 | 多模型并行 | P1 | 同一设备可加载多个模型，按 Pipeline 串联或并行 |
| DI-003 | 前后处理 | P0 | 可配置的预处理（resize/normalize）与后处理（NMS/decode） |
| DI-004 | 推理性能监控 | P0 | FPS、单帧延迟、模型加载时间可上报 |
| DI-005 | 联合推理（可选） | P2 | 低置信度样本上传云端大模型二次判定 |

---

## 6. 非功能需求

| 类别 | 要求 |
| :--- | :--- |
| **性能** | 云平台 API P99 延迟 < 500ms；训练任务调度延迟 < 30s；OTA 下发支持 1000+ 设备并发 |
| **可用性** | 云平台 99.9% SLA（私有化）；设备端离线不影响推理 |
| **可扩展** | 水平扩展训练节点；设备管理支持 10 万级 |
| **安全** | RBAC；通信全链路加密；模型文件加密存储；审计日志不可篡改 |
| **可观测** | 全链路 Trace（OpenTelemetry）；Prometheus + Grafana 监控 |
| **部署** | 支持 K8s 部署（公有/私有云）；最小化部署（单机 Docker Compose）；离线安装 |
| **兼容性** | 浏览器：Chrome/Edge/Firefox 最新 2 版；设备端 OS：Linux (ARM64/x86_64)、RTOS |
| **国际化** | 中文/英文双语 UI |

---

## 7. 约束与假设

### 7.1 约束

- MVP 阶段 GPU 训练资源由客户自行提供（物理机或云 GPU）；
- 设备端推理引擎不自研，复用开源方案（ONNX Runtime / TFLite / NCNN 等）；
- 平台不涉及数据采集硬件的生产与销售。

### 7.2 假设

- 客户具备基础 Linux 运维能力；
- 目标设备已有可用的 BSP（板级支持包）与网络连接能力；
- 训练数据的版权与合规由客户负责。

---

## 8. 里程碑规划（建议）

| 阶段 | 周期 | 关键交付 |
| :--- | :--- | :--- |
| **M1 - MVP** | 12 周 | 云平台核心（项目/数据/模型管理）+ 训练编排 + 单算法容器 + 1 类设备部署 |
| **M2 - 扩展** | 8 周 | 多算法库 + 量化转换 + 灰度部署 + 数据回流 + 多设备型号 |
| **M3 - 增强** | 8 周 | AutoML / 超参搜索 + 预标注 + 联合推理 + 分布式训练 + 大规模设备管理 |
| **M4 - 商业化** | 持续 | 多租户 + 计费 + SaaS + 高级安全审计 + 联邦学习 |

---

## 9. 成功指标

| 指标 | 目标 |
| :--- | :--- |
| 全流程耗时（数据上传 → 设备运行新模型） | ≤ 4 小时（含训练） |
| 训练任务成功率 | ≥ 95% |
| 模型 OTA 成功率 | ≥ 99%（网络正常条件下） |
| 平台 MTBF（平均无故障时间） | ≥ 720 小时 |
| 用户满意度（NPS） | ≥ 40 |

---

## 10. 风险与缓解

| 风险 | 影响 | 缓解措施 |
| :--- | :--- | :--- |
| 异构设备适配工作量大 | 延期 | MVP 限定 3 类硬件，定义清晰的 HAL 接口 |
| GPU 资源不足导致训练排队 | 体验差 | 资源预估工具 + 弹性伸缩 + 训练优先级 |
| 模型转换/量化精度损失 | 效果下降 | 转换后自动回归测试 + 精度阈值告警 |
| 设备网络不稳定 | OTA 失败 | 断点续传 + 本地回滚 + 离线缓存策略 |
| 安全漏洞 | 数据泄露 | 全链路加密 + 定期安全审计 + 最小权限原则 |

---

## 附录 A：与竞品的差异化定位

| 维度 | 本平台 | Edge Impulse | 华为 ModelArts+MindX | 百度 EasyDL+BIE |
| :--- | :--- | :--- | :--- | :--- |
| 部署形态 | **私有化优先** | SaaS 优先 | 公有云 + 私有化 | 公有云 + SDK |
| 硬件绑定 | **无绑定** | Qualcomm 优先 | 昇腾优先 | EdgeBoard 优先 |
| 算法扩展 | **开放容器化** | BYOM（有限制） | MindSpore 优先 | Paddle 优先 |
| 数据主权 | **端侧可不上云** | 必须上云 | 可控 | 可控 |
| 开源度 | **核心开源（规划）** | 闭源 SaaS | 闭源 | 闭源 |
| 训练框架 | **PyTorch/TF/ONNX 中立** | TF/Keras 为主 | MindSpore | PaddlePaddle |
