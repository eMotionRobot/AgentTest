# 云手机 RK3588 资源分配与多实例设计

> 本文档是《云手机rk3588.md》的延伸，重点讨论整机集群中节点间资源共享、跨节点容器、单节点多实例实现方式及资源分配设计。

---

## 一、节点间资源共享问题

### 1.1 核心结论：节点间计算资源不可共享

在 RK3588 整机集群服务器中，**每个 RK3588/RK3588S 节点是一个独立的 SoC，拥有独立的 CPU、GPU、NPU、内存和存储**。节点之间：

| 资源类型 | 是否可跨节点共享 | 说明 |
|----------|-----------------|------|
| CPU | 不可共享 | 每个SoC有独立的8核CPU，无法跨芯片调度 |
| GPU (Mali-G610) | 不可共享 | GPU与SoC紧耦合，无PCIe互联，不支持远程GPU |
| NPU | 不可共享 | NPU为SoC内部IP，无跨节点访问通道 |
| 内存 (LPDDR) | 不可共享 | PoP封装，物理绑定到SoC |
| 本地存储 (eMMC) | 不可共享 | 每节点独立eMMC |
| 网络存储 | 可共享 | 通过NFS/iSCSI等协议实现共享存储 |
| 网络带宽 | 共享上联 | 所有节点共享上联交换机带宽 |

### 1.2 为什么不能像x86服务器那样共享？

x86服务器集群可通过以下技术实现资源共享：
- **NUMA互联**：多CPU通过QPI/UPI互联
- **PCIe Fabric**：GPU通过PCIe/NVLink跨节点
- **RDMA**：高速网络实现远程内存访问

**RK3588 不具备以上任何条件**：
- SoC为单芯片封装，无多芯片互联总线
- 仅有有限的PCIe 3.0通道（RK3588S更少），无fabric能力
- 无RDMA网卡支持
- 内存为PoP封装的LPDDR，不可外部扩展

### 1.3 可以共享的资源

虽然计算资源不可跨节点共享，但以下资源可在集群层面共享：

| 共享资源 | 实现方式 | 用途 |
|----------|----------|------|
| 网络存储 | NFS/Ceph/GlusterFS | 镜像分发、用户数据持久化 |
| 容器镜像仓库 | Docker Registry | 统一镜像管理 |
| 管理控制面 | 集中管控平台 | 调度、监控、编排 |
| 网络出口 | 统一网关/NAT | IP池管理、流量调度 |
| 推流/编码服务 | 集中式推流网关 | WebRTC/RTMP推流聚合 |

---

## 二、跨节点容器问题

### 2.1 核心结论：不支持跨节点运行单个容器

**一个云手机实例（Android容器）无法跨越多个RK3588节点运行。** 原因：

1. **Android系统绑定硬件**：Android运行需要直接访问GPU（Mali-G610）、显示控制器等硬件，这些资源在SoC内部，无法远程访问
2. **容器共享宿主内核**：Docker/LXC容器与宿主机共享Linux内核，进程调度局限于本机CPU
3. **无分布式hypervisor**：不存在可将ARM SoC集群虚拟化为统一资源池的成熟方案
4. **延迟不可接受**：即使理论上实现跨节点内存访问，网络延迟（μs级）远高于本地内存访问（ns级）

### 2.2 容器调度与迁移

虽然不能跨节点运行，但可以实现：

| 能力 | 实现方式 | 说明 |
|------|----------|------|
| 容器调度 | 管控平台/K3s | 根据负载将新实例调度到空闲节点 |
| 冷迁移 | 容器导出+导入 | 停止实例→导出数据→新节点启动 |
| 热迁移 | 基本不可行 | Android容器绑定GPU状态，CRIU对Android支持有限 |
| 故障转移 | 数据层+重建 | 用户数据存网络存储，故障时新节点重建实例 |
| 弹性扩容 | 集群级调度 | 新增节点后自动分配实例 |

### 2.3 集群编排架构

```
┌─────────────────────────────────────────────┐
│              管控平面（Control Plane）         │
│  ┌─────────┐  ┌─────────┐  ┌────────────┐  │
│  │ 调度器   │  │ 监控系统 │  │ 镜像仓库   │  │
│  └─────────┘  └─────────┘  └────────────┘  │
└──────────────────┬──────────────────────────┘
                   │ 管理网络
       ┌───────────┼───────────┐
       ▼           ▼           ▼
┌──────────┐ ┌──────────┐ ┌──────────┐
│ Node-1   │ │ Node-2   │ │ Node-N   │
│ RK3588S  │ │ RK3588S  │ │ RK3588S  │
│┌────────┐│ │┌────────┐│ │┌────────┐│
││实例1   ││ ││实例1   ││ ││实例1   ││
││实例2   ││ ││实例2   ││ ││实例2   ││
││...     ││ ││...     ││ ││...     ││
││实例N   ││ ││实例N   ││ ││实例N   ││
│└────────┘│ │└────────┘│ │└────────┘│
└──────────┘ └──────────┘ └──────────┘
```

---

## 三、单节点多实例实现方式

### 3.1 技术方案对比

在单个 RK3588 节点上实现多个云手机实例，主要有以下技术路线：

| 方案 | 原理 | 代表项目 | 密度 | GPU加速 |
|------|------|----------|------|---------|
| **AIC (Android in Container)** | Docker容器内运行Android | Firefly AIC/redroid | 高（8-12实例） | 支持（共享） |
| **redroid** | Docker化的Android，共享宿主内核 | remote-android/redroid | 高（8-12实例） | 支持（需适配） |
| **轻量级虚拟机** | KVM/QEMU虚拟化 | - | 低（2-4实例） | 有限 |
| **Waydroid** | LXC容器+Wayland | Waydroid | 中（4-6实例） | 支持 |
| **自研容器方案** | 定制化namespace隔离 | 各厂商自研 | 高（10-15实例） | 定制支持 |

### 3.2 AIC/redroid 方案详解（主流方案）

AIC（Android in Container）是RK3588平台最主流的多实例方案：

**核心原理：**
1. 宿主机运行Linux（如Ubuntu/Debian）
2. 通过Docker启动多个Android容器
3. 每个容器运行独立的Android用户空间
4. 所有容器共享宿主机Linux内核
5. GPU通过设备映射（/dev/mali0）实现共享访问

**启动示例（redroid）：**
```bash
docker run -d --name phone1 \
  --privileged \
  --memory 3g \
  --cpus 2 \
  -v /dev/mali0:/dev/mali0 \
  -p 5555:5555 \
  redroid/redroid:12.0.0-arm64
```

### 3.3 关键技术机制

| 机制 | 说明 |
|------|------|
| Linux Namespaces | PID/NET/MNT/UTS隔离，每个实例有独立进程空间 |
| cgroups v2 | CPU、内存、IO资源配额控制 |
| 内核共享 | 所有实例共用宿主Linux内核（减少内存开销） |
| Binder IPC | Android进程间通信，通过binderfs实现多实例隔离 |
| GPU共享 | Mali GPU通过设备节点映射，多进程时分复用 |
| 虚拟显示 | 每个实例创建虚拟framebuffer用于推流 |

---

## 四、资源分配方案设计

### 4.1 RK3588 可分配资源总量

| 资源 | 总量 | 说明 |
|------|------|------|
| CPU核心 | 4x A76 + 4x A55 = 8核 | 大小核异构 |
| 内存 | 8GB / 16GB / 32GB（取决于配置） | LPDDR4x/LPDDR5 |
| GPU | Mali-G610 MP4（4个着色核心） | 无硬件分区能力 |
| NPU | 6 TOPS（3个核心） | 可按核心分配 |
| 视频编码器 | 1路8K或多路1080p/4K | 硬件编码器共享 |
| 视频解码器 | 多路并行解码 | 独立硬件解码器 |
| 存储IO | eMMC/NVMe带宽 | 所有实例共享 |
| 网络IO | 千兆以太网 | 所有实例共享 |

### 4.2 实例资源分配模型

#### 方案A：均分模式（典型8实例/节点）

以 16GB 内存节点为例：

| 资源 | 每实例分配 | 计算 |
|------|-----------|------|
| CPU | 1核（绑定）或 2核（共享） | 8核/8实例=1核，或cpushare权重 |
| 内存 | 1.5GB | (16GB - 2GB系统预留) / 8 ≈ 1.75GB，实际留余量 |
| GPU | 共享（时分复用） | 无法硬件分区，所有实例竞争GPU |
| 存储 | 4-8GB | 系统镜像+用户数据 |
| 网络 | ~120Mbps | 1000Mbps / 8 ≈ 125Mbps |

#### 方案B：差异化分配（按业务需求）

| 实例类型 | CPU | 内存 | GPU优先级 | 适用场景 |
|----------|-----|------|-----------|----------|
| 轻量型 | 1核A55 | 1GB | 低 | 云办公、简单应用 |
| 标准型 | 1核A76+1核A55 | 2GB | 中 | 普通游戏、直播 |
| 性能型 | 2核A76+1核A55 | 3-4GB | 高 | 重度游戏、AI应用 |

#### 方案C：动态弹性分配

```
┌─────────────────────────────────────┐
│         资源管理器（Resource Manager） │
│  ┌──────────┐  ┌─────────────────┐  │
│  │ 负载监控  │  │ 动态调度策略     │  │
│  └──────────┘  └─────────────────┘  │
└──────────────────┬──────────────────┘
                   │
    ┌──────────────┼──────────────┐
    ▼              ▼              ▼
┌────────┐   ┌────────┐   ┌────────┐
│实例1   │   │实例2   │   │实例3   │
│当前:2核 │   │当前:1核 │   │当前:3核 │
│峰值:4核 │   │峰值:2核 │   │峰值:4核 │
└────────┘   └────────┘   └────────┘

策略：空闲时降配，繁忙时升配，总量不超过物理上限
```

### 4.3 CPU 分配策略详解

#### 4.3.1 绑核（CPU Affinity / cpuset）

```bash
# 通过cgroup将实例绑定到特定核心
# 实例1: 绑定 A76-core0 + A55-core0
echo "0,4" > /sys/fs/cgroup/cpuset/phone1/cpuset.cpus

# 实例2: 绑定 A76-core1 + A55-core1  
echo "1,5" > /sys/fs/cgroup/cpuset/phone2/cpuset.cpus
```

**优点**：性能确定性强，避免缓存抖动
**缺点**：灵活性差，资源利用率低

#### 4.3.2 CPU份额（cpu.shares / cpu.weight）

```bash
# cgroups v2: 设置CPU权重
echo 100 > /sys/fs/cgroup/phone1/cpu.weight   # 标准权重
echo 200 > /sys/fs/cgroup/phone2/cpu.weight   # 双倍权重（VIP用户）
```

**优点**：灵活，空闲资源可被借用
**缺点**：繁忙时性能不可预测

#### 4.3.3 CPU配额（cpu.max / CFS Bandwidth）

```bash
# 限制实例1最多使用2核等效CPU时间
# period=100ms, quota=200ms → 最多2核
echo "200000 100000" > /sys/fs/cgroup/phone1/cpu.max
```

**推荐**：生产环境使用 **绑核 + 配额** 组合策略

### 4.4 内存分配策略

| 策略 | 机制 | 说明 |
|------|------|------|
| 硬限制 | memory.max | 超出即OOM Kill，保护其他实例 |
| 软限制 | memory.high | 超出触发内存回收，不kill |
| 预留 | memory.min | 保证最低内存，不被回收 |
| Swap控制 | memory.swap.max | 控制swap使用量 |

**典型配置（2GB实例）：**
```bash
echo 2147483648 > /sys/fs/cgroup/phone1/memory.max      # 硬限2GB
echo 1879048192 > /sys/fs/cgroup/phone1/memory.high     # 软限1.75GB
echo 536870912  > /sys/fs/cgroup/phone1/memory.min      # 预留512MB
echo 0          > /sys/fs/cgroup/phone1/memory.swap.max  # 禁用swap
```

### 4.5 GPU 分配策略

**Mali-G610 不支持硬件级分区（不同于NVIDIA MIG）**，只能通过软件层面管理：

| 策略 | 实现方式 | 效果 |
|------|----------|------|
| 时分复用 | GPU驱动自动调度 | 默认行为，公平但无QoS |
| 优先级调度 | 修改GPU Job优先级 | 高优先级实例获得更多GPU时间 |
| 帧率限制 | 应用层FPS cap | 降低低优先级实例的GPU提交频率 |
| GPU独占 | 特定时间段独占GPU | 适用于重度游戏场景 |
| 无GPU模式 | 纯软件渲染(SwiftShader) | 不需要GPU的实例释放GPU资源 |

**设计建议：**
- 云办公类实例：使用软件渲染，不占GPU
- 轻度游戏：共享GPU + 帧率限制(30fps)
- 重度游戏/直播：高优先级GPU + 不限帧率

### 4.6 存储IO分配

```bash
# cgroups v2 IO控制
# 限制实例1的IO带宽: 读50MB/s，写30MB/s
echo "253:0 rbps=52428800 wbps=31457280" > /sys/fs/cgroup/phone1/io.max

# 限制IOPS: 读1000，写500
echo "253:0 riops=1000 wiops=500" > /sys/fs/cgroup/phone1/io.max
```

### 4.7 网络带宽分配

| 方案 | 工具 | 说明 |
|------|------|------|
| TC (Traffic Control) | tc qdisc/class | Linux内核级QoS |
| 每实例veth + HTB | 每实例独立网络命名空间 | 精确带宽控制 |
| OVS QoS | Open vSwitch | 适合大规模部署 |

---

## 五、设计中需要考虑的关键问题

### 5.1 资源隔离 vs 资源效率的平衡

| 维度 | 强隔离 | 弱隔离（共享） |
|------|--------|--------------|
| 性能确定性 | 高 | 低（noisy neighbor） |
| 资源利用率 | 低（有碎片） | 高 |
| 实例密度 | 低 | 高 |
| 适用场景 | 付费用户、SLA保障 | 免费/低价实例 |
| 实现复杂度 | 低 | 高（需动态调度） |

### 5.2 GPU共享的核心挑战

Mali-G610 的共享面临以下挑战：

1. **无硬件隔离**：不像NVIDIA MIG可物理分区，Mali GPU所有实例共享全部4个着色器核心
2. **内存竞争**：GPU统一内存（与CPU共享LPDDR），多实例同时渲染导致显存带宽争抢
3. **上下文切换开销**：多实例频繁切换GPU上下文，导致性能损失
4. **驱动稳定性**：一个实例的GPU crash可能影响所有实例（驱动级故障）
5. **公平性**：GPU驱动默认FIFO调度，重负载实例可能饿死轻负载实例

**设计应对：**
- 限制同时使用GPU的实例数（如最多4-6个活跃GPU实例）
- 为不需要GPU的实例启用软件渲染
- GPU看门狗：检测GPU hang并reset
- 分级服务：根据用户等级分配GPU时间片

### 5.3 内存超分（Overcommit）设计

| 策略 | 说明 | 风险 |
|------|------|------|
| 不超分 | 所有实例内存总和 ≤ 物理内存 | 最安全，密度低 |
| 适度超分（1.2-1.5x） | 利用实例不同时峰值 | 需要好的OOM策略 |
| 激进超分（>2x） | 依赖swap和内存回收 | ARM平台swap性能差，不推荐 |

**推荐策略**：
- 保留15-20%物理内存给宿主系统
- 实例超分比不超过1.3x
- 启用memory.high软限制实现渐进式回收
- 禁用或严格限制swap（eMMC寿命和性能）

### 5.4 大小核调度策略

RK3588的big.LITTLE架构需要特殊考虑：

| CPU类型 | 核心 | 频率 | 适用任务 |
|---------|------|------|----------|
| 大核 A76 | 4核 | 2.4GHz | 游戏渲染、视频编码、UI响应 |
| 小核 A55 | 4核 | 1.8GHz | 后台服务、轻量计算、待机 |

**调度策略：**
- 前台活跃实例：分配大核（保证用户体验）
- 后台/挂机实例：迁移到小核（省功耗）
- 编码推流任务：优先大核（减少延迟）
- 系统管理进程：固定小核（不抢业务资源）

### 5.5 视频编码资源分配

RK3588 硬件编码器能力：
- 8K@30fps H.265 或 多路 1080p@60fps 并行编码
- 实际可同时编码约 8-10 路 720p@30fps

| 实例推流模式 | 编码配置 | 带宽需求 |
|-------------|----------|----------|
| 720p@30fps | H.265 2Mbps | 2-3 Mbps/实例 |
| 1080p@30fps | H.265 4Mbps | 4-6 Mbps/实例 |
| 1080p@60fps | H.265 6Mbps | 6-8 Mbps/实例 |

**编码器调度设计：**
- 编码会话池：限制同时编码数（如最多10路）
- 优先级队列：VIP用户优先获得编码资源
- 降级策略：负载高时自动降低分辨率/帧率

### 5.6 热管理与降频

高密度部署时，热量是关键限制因素：

```
温度 → 频率映射（Thermal Policy）:
  < 60°C  → 全速运行（A76@2.4GHz）
  60-70°C → 轻度降频（A76@2.0GHz）
  70-80°C → 中度降频（A76@1.6GHz）
  80-85°C → 重度降频 + 减少活跃实例
  > 85°C  → 紧急降频 + 暂停非关键实例
```

**设计要点：**
- 温度与实例调度联动：温度高时减少新实例分配
- 功耗预算（Power Budget）：限制单节点总功耗
- 动态实例密度：根据散热条件调整每节点最大实例数

### 5.7 故障隔离设计

| 故障类型 | 影响范围 | 应对策略 |
|----------|----------|----------|
| 单实例OOM | 该实例 | cgroup memory.max 限制 + OOM Kill |
| GPU Hang | 所有GPU实例 | GPU watchdog + reset + 重启受影响实例 |
| 内核Panic | 整个节点 | BMC自动重启 + 实例自动恢复 |
| 存储满 | 所有实例 | 磁盘配额 + 预警机制 |
| 网络故障 | 该节点所有实例 | 网络冗余 + 实例迁移 |
| 单实例死循环CPU | 所有实例（抢CPU） | cpu.max限制 + 超时kill |

---

## 六、典型部署方案举例

### 6.1 场景：云游戏（16GB节点，8实例）

```yaml
node_config:
  soc: RK3588S
  memory: 16GB
  storage: 128GB eMMC + 256GB NVMe

system_reserved:
  cpu: "A55-core3"          # 1核小核给系统
  memory: 2GB
  storage: 16GB

instance_count: 8
instance_config:
  cpu_policy: "cpuset"      # 绑核
  cpu_allocation:
    - instances[0-3]: "A76-core0,A55-core0"  # 每实例1大核+1小核
    - instances[4-7]: "A76-core1,A55-core1"  # 共享模式
  memory:
    max: 1750MB
    high: 1500MB
    min: 512MB
  gpu:
    mode: "shared"
    max_active_gpu_instances: 6
    priority: "round-robin"
  storage:
    system_image: 4GB (readonly, shared overlay)
    user_data: 8GB (per instance)
  network:
    bandwidth_limit: 10Mbps  # 上行
    veth: per-instance
  encoding:
    resolution: 720p
    fps: 30
    codec: H.265
    bitrate: 2Mbps
```

### 6.2 场景：云办公（16GB节点，12实例）

```yaml
instance_count: 12
instance_config:
  cpu_policy: "cpu.weight"   # 按权重共享
  cpu_weight: 100            # 均等
  memory:
    max: 1024MB
    high: 896MB
  gpu:
    mode: "swiftshader"      # 纯软件渲染，不占GPU
  storage:
    user_data: 4GB
  encoding:
    resolution: 720p
    fps: 15                  # 办公场景低帧率即可
    bitrate: 1Mbps
```

---

## 七、总结

### 关键设计原则

1. **节点即边界**：每个RK3588节点是资源分配的最小物理单元，不可跨节点融合
2. **容器即实例**：通过Linux容器（Docker/LXC）实现多实例，共享内核减少开销
3. **GPU是瓶颈**：Mali GPU无硬件分区，是多实例场景的主要性能瓶颈
4. **大小核善用**：利用big.LITTLE架构实现前台/后台差异化调度
5. **编码器有限**：硬件编码器通道数有限，需要队列化管理
6. **热量是天花板**：实际实例密度受散热能力限制，而非纯计算能力
7. **数据层可共享**：计算层不可跨节点，但数据/镜像/管控可集中

### 与x86云手机方案的对比

| 维度 | RK3588 ARM集群 | x86+GPU服务器 |
|------|---------------|--------------|
| 单节点实例数 | 8-12 | 30-60（取决于GPU） |
| GPU分区 | 不支持 | 支持MIG/vGPU |
| 跨节点共享 | 不支持 | 部分支持（RDMA等） |
| 功耗/实例 | ~1-2W | ~5-10W |
| 成本/实例 | 低 | 高 |
| Android兼容性 | 原生ARM | 需要翻译层或ARM模拟 |
| 适用规模 | 中小规模 | 大规模 |

---

## 参考来源

- Firefly AIC文档: https://wiki.t-firefly.com/en/iCore-3588Q/usage_aic.html
- redroid项目: https://github.com/remote-android/redroid-doc
- redroid-rockchip: https://github.com/redroid-rockchip
- Anbox Cloud容量规划: https://documentation.ubuntu.com/anbox-cloud/explanation/capacity-planning/
- Anbox Cloud资源配置: https://documentation.ubuntu.com/anbox-cloud/explanation/resources/
- Linux cgroups v2文档: https://www.kernel.org/doc/html/latest/admin-guide/cgroup-v2.html
- RK3588 vs RK3588S: https://wiki.radxa.com/Rock5/RK3588_vs_RK3588S

*Content was rephrased for compliance with licensing restrictions.*
