# 端云协同训练推理平台 — 技术分析与软件架构文档

> 文档版本：v1.0 | 日期：2026-05-11 | 状态：初稿

---

## 目录

- [1. 文档说明](#1-文档说明)
- [2. 技术选型分析](#2-技术选型分析)
- [3. 系统总体架构](#3-系统总体架构)
- [4. 云平台详细设计](#4-云平台详细设计)
- [5. 训练管理平台详细设计](#5-训练管理平台详细设计)
- [6. 算法容器详细设计](#6-算法容器详细设计)
- [7. 设备端详细设计](#7-设备端详细设计)
- [8. 数据存储设计](#8-数据存储设计)
- [9. 通信与协议设计](#9-通信与协议设计)
- [10. 安全架构](#10-安全架构)
- [11. 部署架构](#11-部署架构)
- [12. 可观测性设计](#12-可观测性设计)
- [13. 性能与扩展性设计](#13-性能与扩展性设计)
- [14. 技术风险与应对](#14-技术风险与应对)

---

## 1. 文档说明

### 1.1 目的

本文档从技术视角对"端云协同训练推理平台"进行全面分析，包括技术选型依据、系统架构设计、各子系统详细设计、数据流、部署方案与安全策略，为开发团队提供实施指导。

### 1.2 设计原则

| 原则 | 说明 |
| :--- | :--- |
| **松耦合** | 子系统间通过 API/消息队列交互，可独立开发、部署、扩展 |
| **硬件中立** | 不绑定特定芯片/GPU 厂商，通过抽象层适配异构硬件 |
| **云原生** | 容器化 + K8s 编排 + 声明式配置 + 不可变基础设施 |
| **离线优先** | 设备端在断网时仍可正常推理，恢复后同步 |
| **安全纵深** | 通信加密 + 身份认证 + 最小权限 + 审计日志 |
| **可观测** | 全链路 Trace + Metrics + Logs，覆盖云/边/端 |

---

## 2. 技术选型分析

### 2.1 后端技术栈


| 层次 | 技术选型 | 备选方案 | 选型理由 |
| :--- | :--- | :--- | :--- |
| **Web 框架** | Go (Gin/Echo) | Python FastAPI, Java Spring Boot | Go 并发性能优、编译部署简单、云原生生态成熟（K8s/Docker/etcd 皆 Go）；AI 相关用 Python 微服务补充 |
| **API 网关** | Kong / APISIX | Envoy, Traefik | 国产 APISIX 性能高、插件丰富、支持 gRPC 代理 |
| **消息队列** | EMQX (MQTT) + NATS | RabbitMQ, Kafka | EMQX 专业 IoT MQTT Broker，百万连接；NATS 轻量用于内部微服务通信 |
| **异步任务** | Temporal | Celery, Argo Workflows | Temporal 支持长运行 Workflow、重试、可视化，比 Celery 更适合训练编排 |
| **容器编排** | Kubernetes | Docker Swarm | K8s 是行业标准，GPU 调度/CRD/Operator 生态成熟 |
| **GPU 调度** | Volcano (K8s batch scheduler) | Kube-batch | Volcano 是 CNCF 项目，支持 Gang Scheduling、Queue、Fair-share |
| **对象存储** | MinIO | Ceph RGW, SeaweedFS | MinIO 兼容 S3 API、部署简单、性能好，适合私有化 |
| **关系数据库** | PostgreSQL | MySQL | PostgreSQL 支持 JSONB、全文搜索、扩展性好 |
| **缓存** | Redis | Memcached | Redis 兼做缓存/分布式锁/Pub-Sub/限流 |
| **搜索引擎** | Elasticsearch (可选) | Meilisearch | 用于日志搜索、数据集检索，大规模部署时引入 |
| **镜像仓库** | Harbor | Docker Registry | Harbor 支持镜像扫描、复制、RBAC、Helm Chart |

### 2.2 前端技术栈

| 层次 | 技术选型 | 选型理由 |
| :--- | :--- | :--- |
| **框架** | React 18 + TypeScript | 生态最大、组件库丰富、TS 类型安全 |
| **状态管理** | Zustand + React Query | Zustand 轻量替代 Redux；React Query 管理服务端状态 |
| **UI 组件库** | Ant Design 5.x | 企业级组件丰富、中文友好、可定制主题 |
| **图表** | ECharts / Recharts | 训练曲线/设备监控/数据统计可视化 |
| **标注工作台** | 自研 (基于 Fabric.js / Konva.js) | 现有开源标注工具难以深度集成，需定制 |
| **实时通信** | WebSocket + SSE | 训练日志/进度实时推送 |
| **构建工具** | Vite | 开发热更新快、生产构建高效 |

### 2.3 设备端技术栈

| 层次 | 技术选型 | 选型理由 |
| :--- | :--- | :--- |
| **Agent 语言** | Rust (MPU) / C (MCU) | Rust 安全+高性能适合 Linux 边缘设备；C 适合 MCU |
| **MQTT 客户端** | rumqtt (Rust) / Eclipse Paho (C) | 成熟的 MQTT 5.0 实现 |
| **推理引擎** | ONNX Runtime (主) + TFLite / TensorRT / NCNN (按需) | ONNX Runtime 跨平台最广、Execution Provider 机制统一接入 |
| **模型格式** | ONNX (中间格式) | 行业标准交换格式，几乎所有框架可导出 |
| **进程管理** | systemd (Linux) | Agent 作为系统服务运行，崩溃自动重启 |
| **本地存储** | SQLite + 文件系统 | 轻量持久化（任务队列/配置/缓存） |

### 2.4 AI/ML 工具链

| 用途 | 技术选型 | 说明 |
| :--- | :--- | :--- |
| **训练框架** | PyTorch (主) + TensorFlow (兼容) | PyTorch 是当前研究与工业界主流 |
| **模型导出** | torch.onnx.export / tf2onnx | 统一导出为 ONNX |
| **模型优化** | ONNX Runtime Optimizer + 各厂商工具 | 图优化、算子融合 |
| **量化** | ONNX Runtime Quantization / PyTorch Quantization | PTQ + QAT |
| **格式转换** | onnx2tflite / trtexec / mo (OpenVINO) / rknn-toolkit2 | 按目标设备调用 |
| **实验追踪** | MLflow (嵌入训练容器) | 记录超参/指标/产物，平台可对接展示 |

---

## 3. 系统总体架构

### 3.1 四层逻辑架构

```
┌──────────────────────────────────────────────────────────────────┐
│                        用户层 (Clients)                            │
│    Web Console │ CLI │ Mobile App │ OpenAPI │ Webhook 消费者       │
└───────────────────────────────┬──────────────────────────────────┘
                                │
                    ┌───────────▼───────────┐
                    │      API Gateway       │ (Kong/APISIX)
                    │  认证│限流│路由│日志    │
                    └───────────┬───────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                    云平台服务层 (Cloud Services)                    │
│                                                                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐│
│  │Product   │ │Dataset   │ │Model     │ │Deploy    │ │User    ││
│  │Service   │ │Service   │ │Service   │ │Service   │ │Service ││
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └────────┘│
│                                                                    │
└───────────────────────────────┬──────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│               训练管理平台服务层 (Training Platform)                │
│                                                                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐│
│  │Training  │ │Algorithm │ │Convert   │ │Device    │ │OTA     ││
│  │Scheduler │ │Registry  │ │Service   │ │Manager   │ │Service ││
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └────────┘│
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  Temporal (Workflow Engine) │ Volcano (GPU Scheduler)         │ │
│  └──────────────────────────────────────────────────────────────┘ │
└───────────────────────────────┬──────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                  计算层 (Algorithm Containers)                      │
│                                                                    │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐      │
│  │ Training Pod   │  │ Evaluation Pod │  │ Conversion Pod │      │
│  │ (GPU/CPU)      │  │ (CPU/GPU)      │  │ (CPU/GPU)      │      │
│  └────────────────┘  └────────────────┘  └────────────────┘      │
│                                                                    │
└──────────────────────────────────────────────────────────────────┘
                                │
                    ┌───────────▼───────────┐
                    │  EMQX MQTT Broker      │
                    │  (设备接入/指令通道)    │
                    └───────────┬───────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                      设备层 (Edge Devices)                         │
│                                                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ Jetson Orin │  │ RK3588 Box  │  │ STM32 MCU   │   ...        │
│  │ (Agent+RT)  │  │ (Agent+RT)  │  │ (Lite Agent) │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
└──────────────────────────────────────────────────────────────────┘
```

### 3.2 数据流全景

```
[数据采集] ──上传──> [Dataset Service] ──存储──> [MinIO/PostgreSQL]
                                                       │
                                                       ▼
[Training Scheduler] ──创建 Pod──> [Training Container] ──读取数据──> [MinIO]
       │                                    │
       │                                    ├── 指标回调 ──> [Training Scheduler]
       │                                    └── 模型输出 ──> [MinIO]
       │                                                       │
       ▼                                                       ▼
[Convert Service] ──创建 Pod──> [Conversion Container] ──> [Model Artifact in MinIO]
                                                               │
                                                               ▼
[Deploy Service] ──通知──> [OTA Service] ──MQTT/gRPC──> [Device Agent]
                                                               │
                                                               ▼
                                                    [Device 推理运行]
                                                               │
                                                               ▼
                                              [数据回流] ──MQTT──> [Dataset Service]
```



---

## 4. 云平台详细设计

### 4.1 微服务划分

| 服务名 | 职责 | 技术 | 端口 |
| :--- | :--- | :--- | :--- |
| `user-service` | 用户注册/登录/RBAC/OAuth | Go + PostgreSQL | 8001 |
| `product-service` | 产品/项目 CRUD | Go + PostgreSQL | 8002 |
| `dataset-service` | 数据集/样本/标注管理 | Go + PostgreSQL + MinIO | 8003 |
| `model-service` | 模型/版本/产物管理 | Go + PostgreSQL + MinIO | 8004 |
| `deploy-service` | 部署策略/任务/状态追踪 | Go + PostgreSQL + Redis | 8005 |
| `notification-service` | 通知/Webhook/邮件 | Go + Redis + SMTP | 8006 |
| `audit-service` | 操作审计日志 | Go + Elasticsearch | 8007 |
| `gateway` | API 网关/路由/限流/认证 | APISIX | 9080 |
| `web-frontend` | 静态前端资源服务 | Nginx + React SPA | 80/443 |

### 4.2 API 设计规范

```
# URL 结构
/api/v1/{resource}
/api/v1/{resource}/{id}
/api/v1/{resource}/{id}/{sub-resource}

# 示例
GET    /api/v1/projects                    # 列表（分页/筛选）
POST   /api/v1/projects                    # 创建
GET    /api/v1/projects/{id}               # 详情
PUT    /api/v1/projects/{id}               # 更新
DELETE /api/v1/projects/{id}               # 删除
GET    /api/v1/projects/{id}/datasets      # 子资源列表
POST   /api/v1/projects/{id}/trainings     # 创建训练任务
```

**统一响应格式**：
```json
{
  "code": 0,
  "message": "success",
  "data": { ... },
  "pagination": {
    "page": 1,
    "page_size": 20,
    "total": 150
  },
  "request_id": "uuid"
}
```

**错误响应**：
```json
{
  "code": 40001,
  "message": "Dataset not found",
  "details": "Dataset with id 'xxx' does not exist in project 'yyy'",
  "request_id": "uuid"
}
```

### 4.3 认证与授权

```
┌─────────┐     ┌──────────┐     ┌──────────────┐
│  Client │────>│ Gateway  │────>│ user-service │
│         │     │(JWT验证)  │     │(签发/刷新JWT)│
└─────────┘     └──────────┘     └──────────────┘

认证流程：
1. 用户登录 → user-service 签发 Access Token (15min) + Refresh Token (7d)
2. 每次请求携带 Authorization: Bearer {access_token}
3. Gateway 校验 JWT 签名与过期时间
4. 过期后用 Refresh Token 换取新 Access Token

API Key 认证（用于 CLI/Webhook/设备）：
1. 用户在个人中心生成 API Key
2. 请求头：X-API-Key: {key}
3. Gateway 查询 Redis 验证 Key 有效性与权限
```

---

## 5. 训练管理平台详细设计

### 5.1 训练编排 Workflow (Temporal)

```go
// 训练 Workflow 定义（伪代码）
func TrainingWorkflow(ctx workflow.Context, req TrainingRequest) error {
    // Step 1: 资源校验与排队
    err := workflow.ExecuteActivity(ctx, ValidateResources, req)
    
    // Step 2: 准备数据卷
    dataVolume := workflow.ExecuteActivity(ctx, PrepareDataVolume, req.DatasetID)
    
    // Step 3: 拉取算法镜像
    workflow.ExecuteActivity(ctx, PullAlgorithmImage, req.AlgorithmID)
    
    // Step 4: 创建训练 Pod (通过 Volcano)
    podName := workflow.ExecuteActivity(ctx, CreateTrainingPod, TrainingPodSpec{
        Image:      req.AlgorithmImage,
        DataVolume: dataVolume,
        Config:     req.Hyperparameters,
        Resources:  req.ResourceRequirements,
    })
    
    // Step 5: 等待训练完成（长时间运行）
    result := workflow.ExecuteActivity(ctx, WaitForCompletion, podName,
        workflow.WithStartToCloseTimeout(24*time.Hour))
    
    // Step 6: 注册模型到 Model Service
    workflow.ExecuteActivity(ctx, RegisterModel, result.ModelPath, result.Metrics)
    
    // Step 7: 自动触发评估
    workflow.ExecuteActivity(ctx, TriggerEvaluation, result.ModelID)
    
    // Step 8: 通知用户
    workflow.ExecuteActivity(ctx, NotifyUser, req.UserID, result)
    
    return nil
}
```

### 5.2 GPU 调度设计 (Volcano)

```yaml
# Volcano Queue 定义
apiVersion: scheduling.volcano.sh/v1beta1
kind: Queue
metadata:
  name: training-queue
spec:
  weight: 1
  capability:
    nvidia.com/gpu: 8    # 队列最大 GPU 数
    cpu: "64"
    memory: "256Gi"
---
# Volcano Job 定义（训练任务）
apiVersion: batch.volcano.sh/v1alpha1
kind: Job
metadata:
  name: training-{task_id}
spec:
  schedulerName: volcano
  queue: training-queue
  policies:
    - event: PodEvicted
      action: RestartJob
  maxRetry: 2
  tasks:
    - replicas: 1
      name: trainer
      template:
        spec:
          containers:
            - name: algorithm
              image: registry.example.com/algorithms/yolov8:1.2.0
              resources:
                limits:
                  nvidia.com/gpu: 1
                  memory: "16Gi"
                  cpu: "8"
              volumeMounts:
                - name: data
                  mountPath: /data/input
                  readOnly: true
                - name: output
                  mountPath: /data/output
              env:
                - name: TASK_ID
                  value: "{task_id}"
                - name: CALLBACK_URL
                  value: "http://training-scheduler:8010/callback"
```

### 5.3 训练指标采集架构

```
[训练容器] ──HTTP POST──> [Training Scheduler (Callback API)]
     │                              │
     │                              ├── 写入 Redis (实时指标)
     │                              ├── 写入 PostgreSQL (持久化)
     │                              └── WebSocket 推送 → [前端]
     │
     └── stdout/stderr ──> [Fluentd Sidecar] ──> [Elasticsearch]
                                                        │
                                                        ▼
                                              [前端日志查看组件]
```

### 5.4 模型转换服务设计

```
ConvertService
├── POST /api/v1/conversions          # 创建转换任务
├── GET  /api/v1/conversions/{id}     # 查询状态
└── 内部逻辑：
    1. 根据 target_device 确定转换 Pipeline
    2. 创建转换容器 Pod：
       ├── onnx-optimizer (通用优化)
       ├── target-converter (如 trtexec / rknn_toolkit)
       └── benchmark-runner (性能评估)
    3. 转换完成 → 生成 ModelArtifact → 注册到 model-service
```

**目标设备与转换器映射表**：

| 目标设备 | 转换器 | 输出格式 | 量化支持 |
| :--- | :--- | :--- | :--- |
| NVIDIA Jetson | trtexec | .engine (TensorRT) | FP16/INT8 |
| Rockchip RK3588 | rknn-toolkit2 | .rknn | INT8/FP16 |
| Intel x86/NPU | OpenVINO mo | .xml + .bin | FP16/INT8 |
| Android ARM | ncnn/onnx2ncnn | .param + .bin | FP16 |
| STM32 MCU | STM32Cube.AI | .c + .h | INT8 |
| 通用 | onnxruntime | .onnx (优化后) | INT8/FP16 |
| 通用移动端 | TFLite Converter | .tflite | INT8/FP16 |

---

## 6. 算法容器详细设计

### 6.1 容器镜像层次

```
┌─────────────────────────────────────┐
│        算法业务层 (Algorithm)         │  ← 算法工程师编写
│  train.py / model.py / config       │
├─────────────────────────────────────┤
│        平台适配层 (Adapter)           │  ← 平台提供 SDK
│  callback_client.py                 │
│  config_loader.py                   │
│  metric_reporter.py                 │
├─────────────────────────────────────┤
│        基础运行层 (Base Image)        │  ← 平台维护
│  Python 3.10 + PyTorch 2.x          │
│  + CUDA 12.x + cuDNN                │
│  + onnxruntime + common deps        │
└─────────────────────────────────────┘
```

**平台提供的 Base Images**：

| 镜像名 | 用途 | 大小 |
| :--- | :--- | :--- |
| `base/pytorch-gpu:2.3-cuda12` | PyTorch GPU 训练 | ~8GB |
| `base/pytorch-cpu:2.3` | PyTorch CPU 训练 | ~3GB |
| `base/tensorflow-gpu:2.16-cuda12` | TensorFlow GPU | ~9GB |
| `base/onnxrt-cpu:1.18` | ONNX Runtime 推理/转换 | ~2GB |
| `base/converter-tensorrt:8.6` | TensorRT 转换 | ~6GB |
| `base/converter-rknn:2.0` | RKNN 转换 | ~4GB |

### 6.2 平台 SDK（Python）

```python
# edge_ai_sdk/callback.py - 容器内使用的回调 SDK

import os
import requests
import json

class PlatformCallback:
    def __init__(self):
        self.callback_url = os.environ["CALLBACK_URL"]
        self.task_id = os.environ["TASK_ID"]
    
    def report_progress(self, epoch: int, total_epochs: int, 
                        step: int = 0, loss: float = 0.0, **kwargs):
        """上报训练进度"""
        requests.post(f"{self.callback_url}/progress", json={
            "task_id": self.task_id,
            "epoch": epoch,
            "total_epochs": total_epochs,
            "step": step,
            "loss": loss,
            **kwargs
        })
    
    def report_metric(self, metrics: dict):
        """上报评估指标"""
        requests.post(f"{self.callback_url}/metric", json={
            "task_id": self.task_id,
            "metrics": metrics
        })
    
    def report_status(self, status: str, message: str = ""):
        """上报状态变更"""
        requests.post(f"{self.callback_url}/status", json={
            "task_id": self.task_id,
            "status": status,
            "message": message
        })
    
    def load_config(self) -> dict:
        """加载平台下发的配置"""
        with open("/data/input/config.json") as f:
            return json.load(f)
```

### 6.3 算法容器 Dockerfile 示例

```dockerfile
# 目标检测算法容器示例
FROM registry.example.com/base/pytorch-gpu:2.3-cuda12

# 安装算法依赖
COPY requirements.txt /app/
RUN pip install -r /app/requirements.txt

# 拷贝算法代码
COPY src/ /app/src/
COPY algorithm.yaml /app/

# 安装平台 SDK
RUN pip install edge-ai-sdk==1.0.0

# 入口点
WORKDIR /app
ENTRYPOINT ["python", "src/main.py"]
```

---

## 7. 设备端详细设计

### 7.1 Agent 模块架构 (Rust)

```
edge-agent/
├── Cargo.toml
├── src/
│   ├── main.rs                  # 入口、初始化、信号处理
│   ├── config.rs                # 配置加载与热更新
│   ├── communication/
│   │   ├── mod.rs
│   │   ├── mqtt_client.rs       # MQTT 5.0 连接管理
│   │   ├── grpc_client.rs       # gRPC 大文件传输
│   │   └── message_handler.rs   # 指令解析与分发
│   ├── ota/
│   │   ├── mod.rs
│   │   ├── downloader.rs        # 模型下载（断点续传）
│   │   ├── verifier.rs          # SHA256 校验
│   │   └── updater.rs           # 原子切换逻辑
│   ├── inference/
│   │   ├── mod.rs
│   │   ├── runtime_manager.rs   # 推理引擎生命周期管理
│   │   ├── onnx_backend.rs      # ONNX Runtime 封装
│   │   ├── tflite_backend.rs    # TFLite 封装
│   │   ├── tensorrt_backend.rs  # TensorRT 封装
│   │   └── pipeline.rs          # 前/后处理 Pipeline
│   ├── data/
│   │   ├── mod.rs
│   │   ├── collector.rs         # 数据采集策略
│   │   ├── buffer.rs            # 本地环形缓冲区
│   │   └── uploader.rs          # 数据回流上传
│   ├── monitor/
│   │   ├── mod.rs
│   │   ├── health.rs            # 心跳上报
│   │   ├── resource.rs          # CPU/GPU/内存/温度采集
│   │   └── inference_metrics.rs # FPS/延迟统计
│   └── hal/
│       ├── mod.rs
│       ├── camera.rs            # 相机抽象
│       ├── gpio.rs              # GPIO 控制
│       └── sensor.rs            # 传感器抽象
└── tests/
```

### 7.2 推理引擎抽象层

```rust
// src/inference/mod.rs - 推理引擎 Trait 定义

pub trait InferenceBackend: Send + Sync {
    /// 加载模型文件
    fn load_model(&mut self, model_path: &Path, config: &ModelConfig) -> Result<()>;
    
    /// 卸载模型
    fn unload_model(&mut self) -> Result<()>;
    
    /// 执行推理
    fn infer(&self, input: &Tensor) -> Result<Vec<Tensor>>;
    
    /// 获取模型信息
    fn model_info(&self) -> &ModelInfo;
    
    /// 健康检查
    fn health_check(&self) -> bool;
}

pub struct ModelInfo {
    pub name: String,
    pub version: String,
    pub input_shapes: Vec<Vec<i64>>,
    pub output_shapes: Vec<Vec<i64>>,
    pub framework: String,
    pub quantization: String,
}

// 运行时管理器 - 支持热切换
pub struct RuntimeManager {
    active_backend: Arc<RwLock<Box<dyn InferenceBackend>>>,
    standby_backend: Option<Box<dyn InferenceBackend>>,
    model_versions: VecDeque<ModelVersion>,  // 保留最近N个版本
}
```

### 7.3 OTA 原子切换机制

```
模型存储目录结构：
/opt/agent/models/
├── current -> v1.2.0/          # 符号链接指向当前版本
├── v1.2.0/
│   ├── model.onnx
│   ├── config.json
│   └── metadata.json
├── v1.1.0/                     # 保留的旧版本（用于回滚）
│   └── ...
└── staging/                    # 新版本暂存区
    └── v1.3.0/
        └── ...

切换流程：
1. 下载新模型 → staging/v1.3.0/
2. 校验 SHA256
3. 在 standby_backend 中加载 staging/v1.3.0/model.onnx
4. 运行 Sanity Check
5. 原子操作：ln -sfn v1.3.0 current  (符号链接切换)
6. 交换 active_backend 与 standby_backend (RwLock)
7. 上报成功
8. 清理超出保留数的旧版本
```

### 7.4 MCU 精简 Agent 设计

对于 STM32 等 MCU 设备，Agent 极度精简：

```c
// MCU Agent 核心功能（C 实现）
// - 仅支持模型接收（通过串口/SPI/USB从上位机获取）
// - 本地推理（TFLite Micro / TinyMaix）
// - 结果上报（通过 UART 到网关设备）

typedef struct {
    uint8_t model_version;
    uint32_t model_size;
    uint8_t* model_data;        // Flash 中的模型数据
    tflite_context_t* ctx;      // TFLite Micro 上下文
} mcu_agent_t;

// MCU 不直接连网，通过网关设备（如 RPi/RK3588）代理通信
// 网关上运行完整 Agent，通过串口/SPI 管理 MCU 子设备
```



---

## 8. 数据存储设计

### 8.1 存储分层

| 数据类型 | 存储方案 | 说明 |
| :--- | :--- | :--- |
| 业务元数据 | PostgreSQL | 产品/项目/用户/任务/设备等结构化数据 |
| 数据集文件 | MinIO (S3) | 图片/音频/视频原始文件，按 bucket/prefix 组织 |
| 模型文件 | MinIO (S3) | 训练产物、转换产物，按版本归档 |
| 实时缓存 | Redis | 会话/限流/训练进度/设备心跳 |
| 日志 | Elasticsearch | 训练日志/设备日志/审计日志 |
| 时序指标 | Prometheus + VictoriaMetrics | 系统监控/推理性能/设备指标 |
| 消息 | EMQX (持久会话) + NATS (JetStream) | IoT 消息/内部事件流 |

### 8.2 数据库 Schema（核心表）

```sql
-- 项目表
CREATE TABLE projects (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    product_id UUID REFERENCES products(id),
    name VARCHAR(128) NOT NULL,
    task_type VARCHAR(32) NOT NULL,
    description TEXT,
    created_by UUID REFERENCES users(id),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 数据集表
CREATE TABLE datasets (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id),
    name VARCHAR(128) NOT NULL,
    data_type VARCHAR(32) NOT NULL,
    version VARCHAR(32) NOT NULL,
    sample_count INTEGER DEFAULT 0,
    label_stats JSONB,
    storage_bucket VARCHAR(256),
    status VARCHAR(32) DEFAULT 'DRAFT',
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 训练任务表
CREATE TABLE training_tasks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id),
    dataset_id UUID REFERENCES datasets(id),
    algorithm_id UUID REFERENCES algorithms(id),
    status VARCHAR(32) DEFAULT 'CREATED',
    hyperparameters JSONB,
    resource_spec JSONB,
    metrics JSONB,
    started_at TIMESTAMPTZ,
    completed_at TIMESTAMPTZ,
    created_by UUID REFERENCES users(id),
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 模型版本表
CREATE TABLE model_versions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    model_id UUID REFERENCES models(id),
    version VARCHAR(32) NOT NULL,
    status VARCHAR(32) DEFAULT 'TRAINING',
    stage VARCHAR(32) DEFAULT 'DEV',
    training_task_id UUID REFERENCES training_tasks(id),
    metrics JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 模型产物表
CREATE TABLE model_artifacts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    model_version_id UUID REFERENCES model_versions(id),
    format VARCHAR(32) NOT NULL,
    target_device VARCHAR(64),
    quantization VARCHAR(16),
    file_path VARCHAR(512),
    file_size BIGINT,
    performance JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 设备表
CREATE TABLE devices (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(128),
    device_type VARCHAR(64),
    serial_number VARCHAR(128) UNIQUE,
    group_id UUID REFERENCES device_groups(id),
    status VARCHAR(32) DEFAULT 'OFFLINE',
    current_model_version_id UUID REFERENCES model_versions(id),
    last_heartbeat TIMESTAMPTZ,
    hardware_info JSONB,
    tags JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 部署任务表
CREATE TABLE deploy_tasks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id),
    model_version_id UUID REFERENCES model_versions(id),
    artifact_id UUID REFERENCES model_artifacts(id),
    target_group_id UUID REFERENCES device_groups(id),
    strategy VARCHAR(32) DEFAULT 'ALL_AT_ONCE',
    status VARCHAR(32) DEFAULT 'PENDING',
    progress JSONB,
    created_by UUID REFERENCES users(id),
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 8.3 MinIO Bucket 设计

```
buckets/
├── datasets/
│   └── {project_id}/{dataset_id}/{version}/
│       ├── images/
│       ├── labels/
│       └── metadata.json
├── models/
│   └── {project_id}/{model_id}/{version}/
│       ├── source/           # 训练原始产出
│       │   └── best.pt
│       └── artifacts/        # 转换后产物
│           ├── onnx/
│           ├── tensorrt/
│           └── tflite/
├── training-logs/
│   └── {task_id}/
│       └── *.log
└── ota-packages/
    └── {deploy_task_id}/
        └── {device_type}/
            └── model_package.tar.gz
```

---

## 9. 通信与协议设计

### 9.1 内部通信

| 场景 | 协议 | 说明 |
| :--- | :--- | :--- |
| 前端 ↔ 后端 | HTTPS + WebSocket | REST API + 实时推送 |
| 微服务间同步调用 | gRPC | 高性能、强类型、双向流 |
| 微服务间异步事件 | NATS JetStream | 事件驱动（任务状态变更/通知触发） |
| 训练容器 → 平台 | HTTP Callback | 进度/指标/状态上报 |

### 9.2 设备通信

| 场景 | 协议 | QoS | 说明 |
| :--- | :--- | :--- | :--- |
| 心跳上报 | MQTT 5.0 | QoS 0 | 60s 间隔，丢失不影响 |
| 指令下发 | MQTT 5.0 | QoS 1 | 至少一次投递 |
| 模型文件传输 | gRPC Stream / HTTPS | - | 大文件分块、断点续传 |
| 数据回流（小文件） | MQTT 5.0 | QoS 1 | < 256KB 的样本 |
| 数据回流（大文件） | gRPC Stream / HTTPS | - | > 256KB 的文件 |
| 日志流 | MQTT 5.0 | QoS 0 | 非关键，允许丢失 |

### 9.3 MQTT Topic 完整设计

```
# 云端 → 设备（指令）
cloud/cmd/{device_id}/deploy          # 部署模型
cloud/cmd/{device_id}/collect         # 触发数据采集
cloud/cmd/{device_id}/restart         # 重启推理
cloud/cmd/{device_id}/config          # 配置更新
cloud/cmd/{device_id}/ping            # 主动探测

# 设备 → 云端（上报）
device/status/{device_id}/heartbeat   # 心跳
device/status/{device_id}/resource    # 资源使用
device/metrics/{device_id}/inference  # 推理指标
device/data/{device_id}/sample        # 数据回流
device/ota/{device_id}/progress       # OTA 进度
device/ota/{device_id}/result         # OTA 结果
device/log/{device_id}/agent          # Agent 日志

# 广播
cloud/broadcast/announcement          # 全设备广播
cloud/group/{group_id}/cmd            # 设备组指令
```

### 9.4 OTA 包格式

```
model_package.tar.gz
├── manifest.json         # 包元数据
│   {
│     "version": "1.3.0",
│     "model_format": "onnx",
│     "target_device": "jetson_orin",
│     "sha256": "abc123...",
│     "size": 45678901,
│     "min_agent_version": "0.5.0",
│     "config": { ... }
│   }
├── model.onnx            # 模型文件
├── config.json           # 推理配置（前后处理参数）
├── labels.txt            # 类别标签文件
└── test_input.bin        # Sanity Check 测试输入
```

---

## 10. 安全架构

### 10.1 安全分层

```
┌─────────────────────────────────────────────┐
│           应用安全层                          │
│  RBAC │ API Key │ 数据脱敏 │ 审计日志        │
├─────────────────────────────────────────────┤
│           传输安全层                          │
│  TLS 1.3 │ mTLS (设备) │ MQTT over TLS      │
├─────────────────────────────────────────────┤
│           存储安全层                          │
│  数据库加密 │ 模型文件加密 │ 密钥管理(Vault) │
├─────────────────────────────────────────────┤
│           基础设施安全层                      │
│  K8s RBAC │ Network Policy │ Pod Security   │
└─────────────────────────────────────────────┘
```

### 10.2 设备认证流程

```
1. 平台生成设备证书对 (CA 签发)
2. 证书包含：设备 ID、产品 ID、有效期
3. 设备 Agent 使用证书建立 mTLS 连接到 EMQX
4. EMQX 通过 ACL 插件限制设备只能发布/订阅自己的 Topic
5. 证书到期前 Agent 自动申请续期（通过专用 API）
```

### 10.3 模型保护

- 模型文件在 MinIO 中加密存储（AES-256）
- OTA 传输全程 TLS 加密
- 设备端模型文件权限 600，仅 Agent 进程可读
- 可选：模型混淆 / 模型指纹水印（防抄袭）

---

## 11. 部署架构

### 11.1 标准 K8s 部署

```yaml
# 集群节点规划（中等规模参考）
Master Nodes: 3 (高可用)
Worker Nodes (通用): 3-5 (云平台服务 + 训练管理)
Worker Nodes (GPU): 1-N (训练任务，按需扩展)
Storage: Ceph / NFS / 云存储
```

**Namespace 划分**：

| Namespace | 内容 |
| :--- | :--- |
| `edge-ai-platform` | 云平台微服务 |
| `edge-ai-training` | 训练管理 + Volcano + Temporal |
| `edge-ai-jobs` | 训练/转换 Pod（由 Volcano 调度） |
| `edge-ai-infra` | MinIO / PostgreSQL / Redis / EMQX / ES |
| `edge-ai-monitor` | Prometheus / Grafana / Jaeger |
| `edge-ai-gateway` | APISIX / Ingress |

### 11.2 最小化单机部署 (Docker Compose)

适用于 PoC / 开发 / 小团队（单机 + 1 GPU）：

```yaml
# docker-compose.yml (简化)
version: '3.8'
services:
  postgres:
    image: postgres:16
    volumes: [./data/pg:/var/lib/postgresql/data]
    
  redis:
    image: redis:7-alpine
    
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    
  emqx:
    image: emqx/emqx:5.6
    ports: [1883:1883, 8883:8883, 18083:18083]
    
  harbor:
    image: goharbor/harbor-core:v2.10
    # ... (实际需多容器)
    
  gateway:
    image: apache/apisix:3.9
    ports: [9080:9080, 9443:9443]
    
  user-service:
    image: edge-ai/user-service:latest
    
  dataset-service:
    image: edge-ai/dataset-service:latest
    
  model-service:
    image: edge-ai/model-service:latest
    
  training-scheduler:
    image: edge-ai/training-scheduler:latest
    
  deploy-service:
    image: edge-ai/deploy-service:latest
    
  device-manager:
    image: edge-ai/device-manager:latest
    
  web:
    image: edge-ai/web-frontend:latest
    ports: [80:80]
    
  temporal:
    image: temporalio/auto-setup:1.24
    
  # GPU 训练通过 docker run --gpus 单独启动
```

### 11.3 离线部署方案

```
离线安装包结构：
edge-ai-platform-offline-v1.0.tar.gz
├── images/              # 所有 Docker 镜像 tar 包
├── charts/              # Helm Charts
├── tools/               # 安装脚本、K3s 安装包
├── configs/             # 默认配置模板
├── docs/                # 部署文档
└── install.sh           # 一键安装脚本

安装流程：
1. 解压安装包
2. 运行 install.sh → 检测环境 → 安装 K3s/K8s → 导入镜像
3. 配置网络/存储/GPU 驱动
4. Helm install → 启动所有服务
5. 初始化管理员账户
```

---

## 12. 可观测性设计

### 12.1 三大支柱

| 支柱 | 工具 | 覆盖范围 |
| :--- | :--- | :--- |
| **Metrics** | Prometheus + VictoriaMetrics | 系统指标/业务指标/设备指标 |
| **Logs** | Fluentd + Elasticsearch + Kibana | 服务日志/训练日志/设备日志 |
| **Traces** | OpenTelemetry + Jaeger | 跨服务调用链路追踪 |

### 12.2 关键监控指标

**平台层**：
- API 响应时间 (P50/P95/P99)
- 训练任务排队时间 / 成功率
- GPU 利用率 / 内存使用率
- 存储使用量 / IOPS

**设备层**：
- 设备在线率
- 模型推理 FPS / 延迟
- OTA 成功率 / 平均耗时
- 数据回流吞吐量

### 12.3 告警规则示例

```yaml
# Prometheus AlertManager 规则
groups:
  - name: platform
    rules:
      - alert: TrainingTaskStuck
        expr: training_task_duration_seconds > 86400
        for: 5m
        labels: { severity: warning }
        annotations: { summary: "训练任务运行超过24小时" }
        
      - alert: DeviceOffline
        expr: time() - device_last_heartbeat_timestamp > 300
        for: 2m
        labels: { severity: critical }
        annotations: { summary: "设备离线超过5分钟" }
        
      - alert: GPUUtilizationLow
        expr: gpu_utilization_percent < 10
        for: 30m
        labels: { severity: info }
        annotations: { summary: "GPU 利用率过低，资源浪费" }
```

---

## 13. 性能与扩展性设计

### 13.1 水平扩展策略

| 组件 | 扩展方式 | 瓶颈点 |
| :--- | :--- | :--- |
| 云平台微服务 | K8s HPA (CPU/内存) | 无状态，自由扩展 |
| PostgreSQL | 读写分离 + PgBouncer | 写入热点 |
| MinIO | 多节点分布式模式 | 存储容量/IOPS |
| EMQX | 集群模式 (3-5 节点) | 连接数上限 |
| GPU 训练节点 | Cluster Autoscaler | GPU 采购/租赁 |
| Redis | Sentinel / Cluster | 内存容量 |

### 13.2 性能优化策略

**数据上传优化**：
- 前端分片上传 (5MB/片)
- 并发上传 (4 线程)
- 秒传（文件 Hash 去重）
- 服务端直传 MinIO（不经业务服务中转）

**训练任务优化**：
- 数据集预加载到 NVMe SSD (训练节点本地缓存)
- 镜像预拉取 (DaemonSet)
- GPU 共享 (MIG / TimeSlicing) 用于小任务

**设备通信优化**：
- MQTT 消息压缩 (gzip)
- 批量上报（攒批 + 定时 flush）
- 连接池复用

### 13.3 容量规划参考

| 规模 | 设备数 | 并发训练 | 存储 | 计算资源 |
| :--- | :--- | :--- | :--- | :--- |
| 小型 | < 100 | 2-3 | 1TB | 1 GPU + 8C32G |
| 中型 | 100-1000 | 5-10 | 10TB | 4 GPU + 32C128G |
| 大型 | 1000-10000 | 20-50 | 100TB | 16+ GPU + 集群 |
| 超大型 | 10000+ | 100+ | PB 级 | GPU 集群 + 弹性云 |

---

## 14. 技术风险与应对

| 风险 | 概率 | 影响 | 应对策略 |
| :--- | :--- | :--- | :--- |
| 异构设备推理引擎兼容性 | 高 | 高 | 定义 HAL 接口 + 逐设备认证测试 + 社区反馈 |
| 模型转换精度损失 | 中 | 高 | 转换后自动回归测试 + 精度阈值阻断部署 |
| EMQX 百万连接稳定性 | 低 | 高 | 集群部署 + 压测 + 连接池 + 消息限流 |
| Temporal Workflow 故障 | 低 | 高 | Temporal Server 高可用 + Workflow 幂等设计 |
| GPU 资源争抢 | 中 | 中 | Volcano 公平调度 + 优先级队列 + 配额限制 |
| 大文件 OTA 网络中断 | 中 | 中 | 断点续传 + 分块校验 + 自动重试 |
| 安全漏洞（镜像/依赖） | 中 | 高 | Harbor 镜像扫描 + Trivy CI + 定期更新 Base Image |
| 单点故障 | 低 | 高 | 所有有状态组件高可用 + 定期故障演练 |

---

## 附录 A：技术对标分析

基于《主流边缘AI平台调研》，本平台技术路线与业界方案对比：

| 技术维度 | 本平台 | Edge Impulse | 华为 MindX | AWS Greengrass | KubeEdge Sedna |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 训练框架 | PyTorch + TF (容器化，不锁定) | TF/Keras 为主 | MindSpore | SageMaker (多框架) | TF/PyTorch/Paddle |
| 推理运行时 | ONNX RT + 按需扩展 | 自有编译器 | MindSpore Lite + CANN | SageMaker Neo | 不限 |
| 设备通信 | MQTT 5.0 + gRPC | 私有协议 | IEF (私有) | MQTT + Greengrass | KubeEdge (WebSocket) |
| 容器编排 | K8s + Volcano | 不公开 | K8s | Greengrass (Lambda) | K8s + KubeEdge |
| 模型格式 | ONNX (中间) + 多目标 | 多格式导出 | MindIR + ONNX | 多格式 | 不限 |
| 部署形态 | 私有化 K8s / 单机 | SaaS | 华为云 + 私有化 | AWS Cloud | 开源自部署 |
| 开源策略 | 核心开源 (规划) | 闭源 | 闭源 | 闭源 | 完全开源 |

---

## 附录 B：MVP 技术交付清单

| # | 交付物 | 技术要点 |
| :--- | :--- | :--- |
| 1 | 云平台后端 (5 服务) | Go + PostgreSQL + MinIO + Redis |
| 2 | Web 前端 | React + Ant Design + ECharts |
| 3 | 训练编排引擎 | Temporal + Volcano + Callback API |
| 4 | 算法容器示例 (3个) | YOLOv8 检测 + ResNet 分类 + 异常检测 |
| 5 | 模型转换 Pipeline | ONNX 优化 + TensorRT + TFLite |
| 6 | 设备 Agent (Linux ARM64) | Rust + ONNX Runtime + MQTT |
| 7 | EMQX 集群 + Topic 规划 | 设备接入 + 指令 + OTA |
| 8 | 部署工具 | Helm Chart + Docker Compose + 离线包 |
| 9 | 监控体系 | Prometheus + Grafana + 基础告警 |
| 10 | 文档 | API 文档 + 算法接入规范 + 设备接入指南 |
