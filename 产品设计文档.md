# 端云协同训练推理平台 — 产品设计文档

> 文档版本：v1.0 | 日期：2026-05-11 | 状态：初稿

---

## 1. 文档说明

本文档基于《产品需求文档》，对平台四层子系统进行详细的功能设计、交互流程设计与信息架构设计，指导 UI/UX 与开发实现。

---

## 2. 整体信息架构

```
平台首页（Dashboard）
├── 产品中心
│   ├── 产品列表
│   └── 产品详情
│       ├── 项目列表
│       └── 项目详情
│           ├── 数据管理
│           │   ├── 数据集列表
│           │   ├── 数据上传
│           │   ├── 数据标注工作台
│           │   └── 数据统计
│           ├── 模型管理
│           │   ├── 模型仓库
│           │   ├── 模型对比
│           │   └── 模型详情（版本/指标/格式）
│           ├── 训练中心
│           │   ├── 新建训练
│           │   ├── 训练列表
│           │   └── 训练详情（日志/曲线/产物）
│           ├── 部署中心
│           │   ├── 部署策略
│           │   ├── 部署任务
│           │   └── 部署历史
│           └── 设备管理
│               ├── 设备列表
│               ├── 设备分组
│               └── 设备详情（状态/日志/模型）
├── 算法市场
│   ├── 官方算法
│   └── 自定义算法
├── 系统管理
│   ├── 用户与权限
│   ├── 资源管理（GPU/存储配额）
│   ├── 系统配置
│   └── 审计日志
└── 个人中心
    ├── 通知
    └── API Key 管理
```



---

## 3. 云平台功能设计

### 3.1 产品与项目管理

#### 3.1.1 数据模型

```
Product (产品)
├── id: UUID
├── name: String
├── description: String
├── owner: User
├── created_at: DateTime
└── projects: [Project]

Project (项目)
├── id: UUID
├── product_id: FK → Product
├── name: String
├── description: String
├── task_type: Enum (IMAGE_CLASSIFICATION | OBJECT_DETECTION | ANOMALY_DETECTION | AUDIO_CLASSIFICATION | TIMESERIES_CLASSIFICATION | CUSTOM)
├── members: [ProjectMember]
├── datasets: [Dataset]
├── models: [Model]
├── device_groups: [DeviceGroup]
└── deploy_policies: [DeployPolicy]
```

#### 3.1.2 页面设计要点

**产品列表页**
- 卡片式布局，每卡展示：产品名称、项目数、成员数、最近活动时间
- 操作：创建产品、搜索/筛选、编辑、归档

**项目详情页**
- 顶部 Tab 导航：概览 | 数据 | 训练 | 模型 | 部署 | 设备
- 概览面板：数据量趋势、最近训练状态、已部署模型版本、设备在线率

#### 3.1.3 权限模型（RBAC）

| 角色 | 产品 | 项目 | 数据 | 训练 | 模型 | 部署 | 设备 | 系统 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 超级管理员 | CRUD | CRUD | CRUD | CRUD | CRUD | CRUD | CRUD | CRUD |
| 项目管理者 | R | CRUD | CRUD | CRUD | CRUD | CRUD | CRUD | - |
| 算法工程师 | R | R | CRUD | CRUD | CRU | R | R | - |
| 应用开发者 | R | R | R | R | R | CRU | CRU | - |
| 设备运维 | R | R | - | - | R | R | CRUD | - |
| 只读成员 | R | R | R | R | R | R | R | - |

---

### 3.2 数据管理模块

#### 3.2.1 数据模型

```
Dataset (数据集)
├── id: UUID
├── project_id: FK → Project
├── name: String
├── data_type: Enum (IMAGE | AUDIO | TIMESERIES | TEXT | VIDEO)
├── version: String (语义版本号)
├── sample_count: Integer
├── label_stats: JSON  // {label: count, ...}
├── storage_path: String
├── status: Enum (DRAFT | LABELING | READY | ARCHIVED)
└── created_at: DateTime

Sample (样本)
├── id: UUID
├── dataset_id: FK → Dataset
├── file_path: String
├── file_size: Integer
├── labels: [Label]
├── source: Enum (UPLOAD | DEVICE_BACKFLOW | AUGMENTATION)
├── metadata: JSON
└── created_at: DateTime

Label (标注)
├── id: UUID
├── sample_id: FK → Sample
├── label_type: Enum (CLASSIFICATION | BBOX | POLYGON | KEYPOINT | SEGMENT)
├── value: JSON  // 根据 type 不同结构不同
├── annotator: User
└── created_at: DateTime
```

#### 3.2.2 数据上传流程

```
用户选择文件/文件夹
    │
    ▼
前端分片上传（≥100MB 时启用分片）
    │
    ▼
后端接收 → 存入对象存储（MinIO/S3）
    │
    ▼
异步任务：格式校验 + 缩略图生成 + 元数据提取
    │
    ▼
样本写入数据库 → 更新数据集统计
    │
    ▼
（可选）自动触发预标注 Pipeline
```

#### 3.2.3 标注工作台设计

**核心交互**：
- 左侧：样本列表（可筛选未标注/已标注/待审核）
- 中间：主画布（图像/音频波形/时序图），支持缩放、平移
- 右侧：标注工具栏 + 标签列表 + 属性面板
- 底部：快捷键提示 + 进度条

**标注工具**：
- 图像：矩形框、多边形、语义分割画笔、关键点、旋转框
- 音频：时间段选取、事件标记
- 时序：异常区间标记、分类标签

**协作功能**：
- 标注任务分配（管理者将样本批量分配给标注员）
- 审核流程：标注 → 审核通过 / 驳回 → 修改
- 冲突解决：同一样本多人标注时取交集或投票

#### 3.2.4 数据版本管理

- 每次修改（添加样本/修改标注/删除样本）生成新 Snapshot
- 版本号规则：`v{major}.{minor}.{patch}`，用户可手动打标签
- 训练时绑定具体数据集版本，确保可复现
- 支持版本间 diff：新增/删除/修改的样本列表

---

### 3.3 模型管理模块

#### 3.3.1 数据模型

```
Model (模型)
├── id: UUID
├── project_id: FK → Project
├── name: String
├── task_type: Enum
├── versions: [ModelVersion]
└── created_at: DateTime

ModelVersion (模型版本)
├── id: UUID
├── model_id: FK → Model
├── version: String
├── status: Enum (TRAINING | CONVERTING | READY | DEPLOYED | ARCHIVED)
├── source_training_id: FK → TrainingTask
├── source_framework: Enum (PYTORCH | TENSORFLOW | ONNX | CUSTOM)
├── metrics: JSON  // {accuracy, precision, recall, f1, latency_ms, model_size_kb, ...}
├── artifacts: [ModelArtifact]
├── stage: Enum (DEV | STAGING | PRODUCTION)
└── created_at: DateTime

ModelArtifact (模型产物)
├── id: UUID
├── model_version_id: FK
├── format: Enum (ONNX | TFLITE | TENSORRT | OPENVINO | NCNN | CUSTOM)
├── target_device: String  // e.g., "jetson_orin", "rk3588", "stm32h7"
├── file_path: String
├── file_size: Integer
├── quantization: Enum (FP32 | FP16 | INT8 | MIXED)
└── performance: JSON  // {fps, latency_ms, memory_mb}
```

#### 3.3.2 模型对比页面

- 选择 2~4 个模型版本并排对比
- 对比维度：精度指标、模型大小、推理延迟、支持格式
- 可视化：雷达图（多维度一目了然）+ 表格

#### 3.3.3 模型审批流

```
算法工程师提交模型 → STAGING
    │
    ▼
项目管理者审核（查看指标 + 测试集效果）
    │
    ├── 通过 → PRODUCTION（可部署）
    └── 驳回 → DEV（附审核意见）
```

---

### 3.4 部署管理模块

#### 3.4.1 部署策略设计

```
DeployPolicy (部署策略)
├── id: UUID
├── project_id: FK
├── name: String
├── target: DeviceGroupSelector  // 按组/标签/设备ID选择
├── model_version_id: FK → ModelVersion
├── artifact_format: Enum  // 下发哪种格式
├── rollout_strategy: Enum (ALL_AT_ONCE | CANARY | BLUE_GREEN)
├── canary_percentage: Integer  // 灰度比例
├── auto_rollback: Boolean
├── rollback_condition: JSON  // {metric: "accuracy", threshold: 0.85, action: "rollback"}
└── schedule: CronExpression (可选，定时部署)
```

#### 3.4.2 部署流程

```
创建部署任务
    │
    ▼
策略校验（目标设备是否在线/模型格式是否匹配）
    │
    ▼
生成 OTA 包（模型文件 + 配置 + 签名）
    │
    ▼
按策略分批下发
    │
    ├── 设备接收 → 校验 → 加载 → 上报成功
    ├── 设备超时 → 重试（最多3次）→ 标记失败
    └── 灰度期间监控指标异常 → 自动回滚
    │
    ▼
全部完成 → 部署任务状态：SUCCESS / PARTIAL / FAILED
```

#### 3.4.3 部署看板

- 实时进度：已完成 / 进行中 / 失败 / 待重试
- 按设备列表展示各自状态
- 关键指标趋势：部署后精度/FPS 对比部署前



---

## 4. 训练管理平台功能设计

### 4.1 训练任务编排

#### 4.1.1 新建训练向导（3步）

**Step 1：选择数据**
- 选择项目内数据集 + 版本
- 配置训练/验证/测试集划分比例（默认 70/20/10）
- 可选数据增强策略

**Step 2：选择算法**
- 从算法市场选择算法容器
- 配置超参数（根据算法声明的 JSON Schema 动态生成表单）
- 选择计算资源（GPU 型号/数量、CPU/内存配额）

**Step 3：确认与提交**
- 预览配置摘要
- 预估训练时长（基于历史数据）
- 提交 → 进入队列

#### 4.1.2 训练任务状态机

```
CREATED → QUEUED → PREPARING → RUNNING → [SUCCESS | FAILED | CANCELLED | TIMEOUT]
   │                                          │
   │         用户取消                          │ 自动：Early Stop
   └──────────────────────────────────────────┘
```

各状态说明：
- **CREATED**：任务已创建，等待资源校验
- **QUEUED**：等待 GPU/CPU 资源分配
- **PREPARING**：拉取镜像、挂载数据卷、初始化环境
- **RUNNING**：训练进行中，实时推送日志与指标
- **SUCCESS**：训练完成，产出模型文件已注册到模型仓库
- **FAILED**：训练异常退出，保留日志供排查
- **CANCELLED**：用户主动取消
- **TIMEOUT**：超过最大训练时长，强制终止

#### 4.1.3 训练详情页设计

- **顶部**：任务名称、状态徽章、持续时间、资源消耗
- **Tab 1 - 训练曲线**：Loss/Accuracy/LR 实时折线图，可叠加多 Run 对比
- **Tab 2 - 日志**：实时滚动日志，支持级别过滤（INFO/WARN/ERROR）与搜索
- **Tab 3 - 配置**：本次训练的完整配置快照（数据集版本、算法版本、超参数、资源）
- **Tab 4 - 产物**：训练产出的模型文件、评估报告、可视化样例

#### 4.1.4 超参数搜索设计

```
HyperparamSearch (超参搜索任务)
├── id: UUID
├── base_training_config: JSON
├── search_space: JSON  // {"learning_rate": {"type": "log_uniform", "min": 1e-5, "max": 1e-2}, ...}
├── strategy: Enum (GRID | RANDOM | BAYESIAN)
├── max_trials: Integer
├── objective_metric: String  // "val_accuracy"
├── objective_direction: Enum (MAXIMIZE | MINIMIZE)
├── trials: [Trial]
└── best_trial_id: UUID
```

- 搜索结果以散点图/平行坐标图展示
- 最优 Trial 可一键提升为正式模型版本

---

### 4.2 算法市场设计

#### 4.2.1 算法注册规范

每个算法容器需提供 `algorithm.yaml` 描述文件：

```yaml
name: yolov8-detection
version: "1.2.0"
display_name: "YOLOv8 目标检测"
description: "基于 Ultralytics YOLOv8 的通用目标检测算法"
task_type: OBJECT_DETECTION
framework: PYTORCH

inputs:
  data_dir: "/data/input"          # 平台挂载数据集目录
  config_file: "/data/config.json" # 平台生成的配置文件

outputs:
  model_dir: "/data/output/model"  # 模型产物输出目录
  metrics_file: "/data/output/metrics.json"  # 评估指标
  logs_dir: "/data/output/logs"    # 训练日志

hyperparameters:
  - name: epochs
    type: integer
    default: 100
    min: 1
    max: 1000
    description: "训练轮次"
  - name: learning_rate
    type: float
    default: 0.001
    min: 0.00001
    max: 0.1
    description: "初始学习率"
  - name: batch_size
    type: integer
    default: 16
    enum: [4, 8, 16, 32, 64]
    description: "批次大小"
  - name: image_size
    type: integer
    default: 640
    enum: [320, 416, 512, 640, 1280]
    description: "输入图像尺寸"
  - name: pretrained
    type: boolean
    default: true
    description: "是否使用预训练权重"

resources:
  gpu_required: true
  min_gpu_memory_gb: 4
  min_cpu_cores: 4
  min_memory_gb: 8

callbacks:
  progress_endpoint: "http://localhost:8080/progress"  # Agent 注入
  metric_endpoint: "http://localhost:8080/metric"
```

#### 4.2.2 算法市场页面

- 分类浏览：图像分类 | 目标检测 | 语义分割 | 异常检测 | 音频 | 时序 | 自定义
- 每个算法卡片：名称、版本、框架图标、支持的任务类型、使用次数、评分
- 详情页：算法说明、超参数列表、性能基准、示例效果、变更日志

---

### 4.3 模型转换与优化

#### 4.3.1 转换 Pipeline 设计

```
源模型 (PyTorch/TF/Keras)
    │
    ▼
[Step 1] 导出为 ONNX (标准中间格式)
    │
    ▼
[Step 2] ONNX 优化 (图优化、算子融合)
    │
    ▼
[Step 3] 目标格式转换 (按目标设备选择)
    ├── TFLite (Android/MCU/Coral)
    ├── TensorRT (NVIDIA Jetson/GPU)
    ├── OpenVINO (Intel CPU/NPU)
    ├── NCNN (通用 ARM/手机)
    ├── RKNN (RK3588/RK3566)
    └── 其他自定义格式
    │
    ▼
[Step 4] 量化 (可选)
    ├── PTQ (后训练量化，需校准数据集)
    └── QAT (量化感知训练，需重新训练)
    │
    ▼
[Step 5] 性能评估
    ├── 精度回归测试 (与源模型对比)
    └── 速度/内存 Benchmark
    │
    ▼
输出：ModelArtifact + 性能报告
```

#### 4.3.2 转换配置页面

- 选择源模型版本
- 选择目标设备类型（下拉列表含所有已注册设备型号）
- 选择量化策略（FP32 / FP16 / INT8 / 混合精度）
- 上传校准数据集（INT8 量化时需要）
- 提交 → 生成转换容器任务

---

### 4.4 设备通信管理

#### 4.4.1 设备注册流程

```
管理员在平台生成「设备接入凭证」(证书 + 配置文件)
    │
    ▼
现场人员将凭证烧录/拷贝到设备
    │
    ▼
设备 Agent 启动 → 读取凭证 → 连接平台 MQTT Broker
    │
    ▼
Agent 上报设备指纹（硬件型号/OS/序列号/MAC/算力信息）
    │
    ▼
平台校验凭证 → 设备状态变为「在线」
    │
    ▼
管理员分配到设备组 → 设备可接收部署任务
```

#### 4.4.2 设备通信协议设计

**通道**：MQTT 5.0（主通道）+ gRPC（大文件传输/流式日志）

**Topic 设计**：

| Topic | 方向 | 用途 |
| :--- | :--- | :--- |
| `cloud/{device_id}/command` | 云→端 | 指令下发（部署/重启/采集等） |
| `device/{device_id}/status` | 端→云 | 心跳与状态上报 |
| `device/{device_id}/metrics` | 端→云 | 推理指标上报 |
| `device/{device_id}/data` | 端→云 | 数据回流（小文件） |
| `cloud/{device_id}/ota` | 云→端 | OTA 通知（大文件走 gRPC/HTTP） |
| `device/{device_id}/ota_ack` | 端→云 | OTA 进度与结果反馈 |

**消息格式**（JSON）：

```json
{
  "msg_id": "uuid",
  "timestamp": 1715400000,
  "type": "DEPLOY_MODEL",
  "payload": {
    "model_version_id": "xxx",
    "artifact_url": "https://...",
    "sha256": "...",
    "config": {...}
  }
}
```

#### 4.4.3 设备管理页面

**设备列表**：
- 表格展示：设备名称、型号、分组、在线状态、当前模型版本、最后心跳时间
- 批量操作：批量分组、批量部署、批量重启

**设备详情页**：
- 基本信息：硬件型号、OS、IP、序列号、接入时间
- 实时状态：CPU/GPU/内存/温度 仪表盘（5s 刷新）
- 模型历史：当前模型 + 历史部署记录
- 日志：Agent 日志流（实时 + 历史查询）
- 推理监控：FPS / 延迟 / 推理计数 曲线图



---

## 5. 算法容器设计

### 5.1 容器分类与职责

| 容器类型 | 触发方式 | 输入 | 输出 | 生命周期 |
| :--- | :--- | :--- | :--- | :--- |
| 训练容器 | 用户提交训练任务 | 数据目录 + 配置 | 模型文件 + 指标 | 任务级（完成即销毁） |
| 评估容器 | 训练完成后自动触发 | 模型 + 测试集 | 评估报告 JSON | 任务级 |
| 转换容器 | 用户触发模型转换 | 源模型 + 目标配置 | 目标格式模型 + 性能报告 | 任务级 |
| 推理服务容器 | 按需启动 | 模型 + 输入数据 | 推理结果 | 常驻 / 按需 |

### 5.2 容器标准接口

#### 5.2.1 目录约定

```
/data/
├── input/
│   ├── dataset/          # 训练/评估数据（只读挂载）
│   │   ├── images/
│   │   ├── labels/
│   │   └── metadata.json
│   ├── model/            # 评估/转换时的输入模型
│   └── config.json       # 平台生成的配置（超参数+元信息）
├── output/
│   ├── model/            # 产出模型文件
│   │   ├── best.pt / best.onnx
│   │   └── model_info.json
│   ├── metrics.json      # 标准评估指标
│   ├── logs/             # 训练日志
│   └── artifacts/        # 其他产物（可视化图片等）
└── cache/                # 预训练权重缓存（可选持久卷）
```

#### 5.2.2 环境变量

| 变量名 | 说明 |
| :--- | :--- |
| `TASK_ID` | 当前任务唯一 ID |
| `TASK_TYPE` | TRAIN / EVALUATE / CONVERT / INFER |
| `CALLBACK_URL` | 进度与指标上报地址 |
| `GPU_COUNT` | 分配的 GPU 数量 |
| `MAX_EPOCHS` | 最大训练轮次（兜底） |
| `EARLY_STOP_PATIENCE` | 早停耐心值 |

#### 5.2.3 回调接口（容器内调用）

**上报训练进度**：
```
POST {CALLBACK_URL}/progress
Body: {"epoch": 10, "total_epochs": 100, "step": 500, "loss": 0.23}
```

**上报评估指标**：
```
POST {CALLBACK_URL}/metric
Body: {"accuracy": 0.95, "precision": 0.93, "recall": 0.92, "f1": 0.925, "map50": 0.87}
```

**上报状态变更**：
```
POST {CALLBACK_URL}/status
Body: {"status": "COMPLETED", "message": "Training finished successfully"}
```

### 5.3 容器编排策略

#### 5.3.1 资源调度

```
任务提交
    │
    ▼
调度器检查资源池（GPU 空闲数、内存余量）
    │
    ├── 资源充足 → 立即调度，创建 Pod/Container
    └── 资源不足 → 进入优先级队列
                    │
                    ├── P0 (生产热修) → 抢占低优先级任务
                    ├── P1 (正常训练) → FIFO 等待
                    └── P2 (实验探索) → 低优先级，空闲时执行
```

#### 5.3.2 容器安全隔离

- 每个训练任务运行在独立 Namespace / Pod
- 网络策略：容器仅可访问数据卷 + 回调 URL，不可访问外部网络
- 文件系统：input 目录只读，output 目录读写
- 资源限制：CPU/Memory/GPU Limit 强制生效
- 运行时安全：non-root 运行，禁用特权模式

---

## 6. 设备端设计

### 6.1 Agent 架构

```
┌─────────────────────────────────────────────┐
│              Device Agent                      │
├─────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 通信模块 │  │ OTA 模块 │  │ 数据模块 │  │
│  │(MQTT/gRPC)│  │(下载/校验 │  │(采集/缓存│  │
│  │          │  │ /切换)   │  │ /上传)   │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  │
│       │              │              │        │
│  ┌────▼──────────────▼──────────────▼────┐  │
│  │           核心调度器 (Scheduler)        │  │
│  └────────────────────┬──────────────────┘  │
│                       │                      │
│  ┌────────────────────▼──────────────────┐  │
│  │       推理引擎管理器 (Runtime Manager)  │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ │  │
│  │  │ONNX RT  │ │TFLite   │ │TensorRT │ │  │
│  │  └─────────┘ └─────────┘ └─────────┘ │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  ┌──────────────────────────────────────┐   │
│  │         硬件抽象层 (HAL)              │   │
│  │  Camera │ GPIO │ Sensor │ NPU │ Storage│  │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

### 6.2 Agent 核心流程

#### 6.2.1 启动流程

```
Agent 进程启动
    │
    ▼
加载本地配置文件 (device_config.yaml)
    │
    ▼
初始化硬件抽象层 (检测可用推理后端)
    │
    ▼
加载本地缓存模型 (若有) → 启动推理
    │
    ▼
连接云端 MQTT Broker (证书认证)
    │
    ├── 成功 → 上报设备状态 + 订阅指令 Topic
    └── 失败 → 离线模式运行，定期重试
```

#### 6.2.2 模型更新流程（OTA）

```
收到 OTA 通知 (MQTT)
    │
    ▼
解析：模型 URL + SHA256 + 目标配置
    │
    ▼
下载模型包 (gRPC/HTTPS，支持断点续传)
    │
    ▼
校验完整性 (SHA256)
    │
    ├── 校验失败 → 重新下载 (最多3次) → 上报失败
    │
    ▼
存入暂存目录
    │
    ▼
加载新模型到推理引擎 (新实例)
    │
    ▼
运行 Sanity Check (用预设测试样本验证)
    │
    ├── 验证失败 → 回滚到旧模型 → 上报失败
    │
    ▼
原子切换：新模型替换旧模型 (零停机)
    │
    ▼
上报成功 + 新模型版本号
    │
    ▼
清理旧模型文件 (保留最近 N 个版本用于回滚)
```

#### 6.2.3 数据回流策略

| 策略类型 | 触发条件 | 说明 |
| :--- | :--- | :--- |
| 定时采样 | 每 N 分钟 | 定期上传最近 N 张推理输入 |
| 低置信度 | 推理置信度 < 阈值 | 可能误判的样本优先回流 |
| 异常触发 | 推理结果异常/新类别 | 主动学习场景 |
| 手动触发 | 云端指令 | 管理员按需采集 |
| 带宽感知 | 网络空闲时 | 避免影响推理时延 |

### 6.3 设备端配置文件

```yaml
# device_config.yaml
device:
  id: "device-001"
  name: "生产线A-检测相机"
  type: "jetson_orin_nano"
  
connection:
  broker: "mqtts://platform.example.com:8883"
  cert_path: "/etc/agent/certs/"
  reconnect_interval_s: 30
  heartbeat_interval_s: 60

inference:
  runtime: "onnxruntime"  # onnxruntime | tflite | tensorrt | ncnn
  model_dir: "/opt/agent/models/"
  max_model_versions: 3
  input_source: "camera"  # camera | file | rtsp | sensor
  
data_backflow:
  enabled: true
  strategy: "low_confidence"
  confidence_threshold: 0.6
  max_samples_per_hour: 100
  upload_batch_size: 10
  
resources:
  max_cpu_percent: 80
  max_memory_mb: 2048
  gpu_memory_fraction: 0.8
```

---

## 7. 核心业务流程（端到端）

### 7.1 完整训练部署流程

```
[算法工程师]                    [训练管理平台]              [设备端]
     │                              │                        │
     │ 1. 上传/标注数据              │                        │
     │─────────────────────────────>│                        │
     │                              │                        │
     │ 2. 选择算法 + 配置超参        │                        │
     │─────────────────────────────>│                        │
     │                              │                        │
     │                    3. 创建训练容器，执行训练             │
     │                              │                        │
     │ 4. 查看训练进度/曲线          │                        │
     │<─────────────────────────────│                        │
     │                              │                        │
     │ 5. 训练完成，模型注册          │                        │
     │<─────────────────────────────│                        │
     │                              │                        │
     │ 6. 触发模型转换（目标格式）    │                        │
     │─────────────────────────────>│                        │
     │                              │                        │
     │ 7. 审核模型 → PRODUCTION      │                        │
     │─────────────────────────────>│                        │
     │                              │                        │
     │ 8. 创建部署策略，下发          │                        │
     │─────────────────────────────>│ ──── 9. OTA ──────────>│
     │                              │                        │
     │                              │ <── 10. 部署成功上报 ───│
     │                              │                        │
     │ 11. 查看部署状态              │                        │
     │<─────────────────────────────│                        │
     │                              │                        │
     │                              │ <── 12. 推理指标上报 ───│
     │                              │                        │
     │                              │ <── 13. 数据回流 ───────│
     │                              │                        │
     │ 14. 用回流数据迭代训练         │                        │
     │─────────────────────────────>│                        │
```

### 7.2 数据闭环流程

```
设备采集数据 → 按策略回流到云端
    │
    ▼
自动进入「待标注」队列
    │
    ▼
人工/自动标注 → 合入数据集新版本
    │
    ▼
触发增量训练 / 重新训练
    │
    ▼
新模型产出 → 评估 → 审批 → 部署
    │
    ▼
模型效果提升 → 循环迭代
```

---

## 8. 交互设计原则

| 原则 | 说明 |
| :--- | :--- |
| **渐进式复杂度** | 默认展示简洁配置，高级选项折叠/展开 |
| **实时反馈** | 训练进度/部署状态秒级更新，避免用户反复刷新 |
| **错误可恢复** | 任何失败操作提供明确原因 + 重试按钮 + 日志链接 |
| **批量效率** | 数据上传/设备管理/部署均支持批量操作 |
| **上下文关联** | 从训练详情可跳转到数据集、从模型可跳转到训练任务 |
| **专业与易用平衡** | 算法工程师看到完整参数；应用开发者看到一键部署 |

---

## 9. 通知与告警设计

| 事件 | 通知渠道 | 接收者 |
| :--- | :--- | :--- |
| 训练完成/失败 | 站内信 + 邮件 + Webhook | 任务创建者 |
| 模型审批请求 | 站内信 + 邮件 | 项目管理者 |
| 部署完成/失败 | 站内信 + 邮件 + Webhook | 部署操作者 + 设备运维 |
| 设备离线 | 站内信 + 短信(可选) | 设备运维 |
| 推理精度下降 | 站内信 + 邮件 | 算法工程师 + 项目管理者 |
| 资源配额不足 | 站内信 | 平台管理员 |
| 安全告警 | 短信 + 邮件 | 平台管理员 |

---

## 10. 开放能力设计

### 10.1 API 设计原则

- RESTful API（资源操作）+ WebSocket（实时推送）+ gRPC（高性能内部通信）
- OpenAPI 3.0 规范，自动生成 SDK（Python / Go / TypeScript）
- API 版本化：`/api/v1/...`
- 统一错误码体系 + Rate Limiting + 分页

### 10.2 Webhook 集成

支持用户配置 Webhook URL，在以下事件发生时推送通知：
- 训练完成/失败
- 模型审批通过
- 部署完成
- 设备状态变更

### 10.3 CLI 工具

提供命令行工具 `edge-ai-cli`：
```bash
# 登录
edge-ai-cli login --server https://platform.example.com

# 上传数据
edge-ai-cli dataset upload --project my-project --path ./images/

# 提交训练
edge-ai-cli train create --algorithm yolov8 --dataset v1.2 --epochs 100

# 查看训练状态
edge-ai-cli train status --id <task_id>

# 下载模型
edge-ai-cli model download --id <model_id> --format onnx

# 部署
edge-ai-cli deploy create --model <version_id> --device-group production
```
